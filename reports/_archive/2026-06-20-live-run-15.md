# Live run #15 — end-to-end re-run: the downstream wall is in-lab navigation (2026-06-20)

_The conclusive re-run of run #14 after (a) topping up the Anthropic credits and (b) building the
API-error circuit breaker. Run #14 reached Oak's lab but credits died mid-dialog, leaving "where does
the integrated agent break downstream of the lab?" unanswered. This run answers it._

**TL;DR — credits were masking an INTERIOR-NAVIGATION wall.** With credits healthy (0 errors, 0 false
breaker trips), the agent again reached **Oak's lab cold (`38→37→0→40` by step 130)** — then got
**wall-locked navigating the lab interior to reach Oak**, never got the starter, never reached the
rival battle. **All 100 LLM wakes were `[wake:stuck]`** — the pose-based occupancy map drifts/corrupts
in the tight lab room, so the free autopilot ran out of (real) frontier and the LLM flailed on a map it
itself called *"corrupt."* Budget-cap halt at 100 wakes (~$0.6–0.8). **The downstream bottleneck is
interior / short-range navigation — the deferred dead-reckoning drift, now THE blocker to forward
progress.** Two infra wins also landed: the circuit breaker stayed correctly silent (first live proof it
doesn't interfere when healthy), and the run-end auto-report fired.

## Config
```
play_pokemon.py --brain hybrid --perception --no-vision --backend aria --load-state start.state \
    --steps 1000 --max-llm-calls 100 --stuck-steps 250 --out runs/run15 --record runs/run15.mp4
```
Clean start: `reset_aria_memory.py --yes` (archive `iter-015_2026-06-20.zip`). Same config as run #14 +
the new API-error circuit breaker; credits topped up beforehand.

## Results (oracle-verified — auto-extracted; do not hand-edit the numbers)

| Metric | Value |
| --- | --- |
| Outcome | no battle this run (wall-locked in the lab; never got the starter) |
| `in_battle` | start 0, values [0], sustained-exit @ — (never reached the rival battle) |
| Maps (trajectory) | 38→37→0→40 (house → Pallet → **Oak's lab**), first-seen 40 @ step 130 |
| Oracle rows (steps) | 545 |
| LLM wakes | **100 — ALL `[wake:stuck]`** (0 productive "mode" wakes) |
| Auto-advances (free) | 148 (Oak intro dialog) |
| Errors (400 / crash / credit) | **0** (credits healthy; breaker never tripped) |
| Episode summary | 100/545 wakes (18.3%), reward 3.0 |
| Halt | budget cap (100 LLM calls) |
| Cost | ~$0.6–0.8 (100 wakes, all unproductive — the cost of confirming the nav wall) |

## What worked

- **Cold-start nav to the lab is reliable** (`38→37→0→40` by step 130, third time: runs #5, #14, #15) —
  the Phase B place-graph handles the house→Pallet→lab *inter-map* path cleanly every time.
- **The circuit breaker behaved correctly — it stayed SILENT.** Credits were healthy, so no wake
  errored, `consec_api_errors` never left 0, and the breaker never tripped. First live confirmation it
  doesn't false-fire / interfere on a healthy run (the trip path is unit-tested).
- **The run-end auto-report hook fired** again, correctly (`errors: 0`, `wakes: 100`, trajectory all
  right) — the reporting infra is solid across runs.

## What broke / the bottleneck (the answer we ran for)

- **Interior / short-range navigation corrupts in the lab room → can't reach Oak → no starter.** Inside
  the small, wall-dense lab the pose-only dead-reckoning map drifts; the agent physically wanders the
  room (real RAM movement) but its *internal* occupancy/frontier model is wrong, so it can't
  purposefully walk to Oak's tile to trigger the starter selection. It burned **all 100 wakes** as
  `[wake:stuck]`, the autopilot having exhausted its (corrupted) frontier. The agent self-diagnoses it:
  *"geometry corruption," "the wall map is corrupt — reset and use GOTO pathfinder exclusively,"* and a
  pose/belief desync (*"current (-3,1) reporting is lag/desync; the real game state is at Oak's starter
  room"* — it confabulated having reached Oak while the pose said otherwise). **This is the residual
  Phase-B gap (the DEFERRED dead-reckoning/odometry drift), now the #1 blocker.** The place-graph fixed
  *between*-map navigation; *within*-room navigation is still pose-only and drifts.
- **The stuck-step watchdog was placated by aimless-but-real wandering.** Because the agent physically
  moved around the room, RAM coverage `(map,x,y)` kept ticking, so the no-progress fingerprint reset and
  `--stuck-steps 250` never fired — the budget cap had to stop it. (Real movement ≠ real progress; the
  watchdog can't tell "exploring new tiles toward a goal" from "wandering a room it can't model.")

## Next

1. **Fix interior / short-range navigation — the deferred dead-reckoning drift.** The place-graph solved
   inter-map; intra-room pose drift is now the hard blocker (it stops the starter → rival → Route 1, and
   it also defeats the watchdog). Options to weigh: the measured-distance odometry the Phase-B notes
   deferred (needs a controller that understands variable step sizes), an interior re-grounding signal,
   or a tighter occupancy reset on entering a small room. This is the precondition for ANY further
   forward progress.
2. The **learned blind-execute battle policy** stays queued but is now clearly *behind* nav: the battle
   is already solved (run #12/#13), and the agent can't reach a battle from a cold start until it can
   get the starter. Nav first.

---
_Artifacts: video `runs/run15.mp4`; oracle `runs/run15/oracle.jsonl`; final state `runs/run15_end.state`;
archive `aria_memory_archives/iter-015_2026-06-20.zip`. Spend ~$0.6–0.8 (100 stuck wakes). The circuit
breaker (committed this session) stayed silent — credits were healthy; it's validated as non-intrusive._
