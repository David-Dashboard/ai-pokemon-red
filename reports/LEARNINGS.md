# Learnings log — Red / ai-aria generalization

Running, per-iteration record (newest insights kept; a consolidated final report is assembled from
this at the end). North star: a **simple, generalizable** agent that acts on the world — generalize
across games → reality, no ROM/privileged state, **cheap** (minimal API). Pokémon Red is the probe.

---

## Iteration 01 — baseline: small LLM on raw pixels
- **What:** decoupled aria as the brain (`--backend aria`), repurposed the companion into "Red", ran Haiku on raw 160×144 frames.
- **Result:** stuck in the first room; **confabulated** (invented NPCs from furniture, mislabeled the bedroom "Oak's Lab", and wrote a *fabricated* "I exited the lab" into long-term memory).
- **Learning:** **Perception is the bottleneck, not planning.** Anything from RAM was correct; anything read from pixels was hallucinated — and a *faithful memory system amplifies bad perception* (it immortalizes the hallucination). ⇒ build a perception module; demote RAM to a scoring oracle.

## Iteration 02 — the perception module
- **What:** a role-named `SymbolicState` seam (`perceive(frame) → state`, RAM → non-leaking `oracle.jsonl`); then odometry + an occupancy map (frame-diff "did my move work?" + dead-reckoned visited/walls/frontiers).
- **Result:** **it left the bedroom** (map 38→37) — the thing baseline never did.
- **Learnings:**
  - A near-vision-free signal (frame-diff + a remembered map) is enough to navigate; you don't need to *recognize* tiles to *not loop*.
  - Role-named schema (pose / spatial_memory / affordances / last_action / confidence) = a robot's belief state ⇒ generalizes; tile-specifics hide *behind* the roles.

## Iteration 03 — measure, make it cheap, make it loop-safe
- **Measurement rig (free):** scores perception vs the oracle with a scripted brain — **no API**. Walkability **100%** (single-tile), then **99.3%** after tuning.
- **Free autopilot:** frontier exploration + BFS, **0 LLM calls** — explored **3 maps / 44 cells for $0**, where baseline burned ~100 calls and never left one room. ⇒ **the cost win: take routine movement off the LLM.**
- **Area-detection + oracle-tuned thresholds:** the tuner *honestly* showed diff-magnitude can't perfectly separate map-transitions from big in-map moves (fade-detection is the proper future signal). Odometry undercounts (~1.7 real tiles/move) under multi-tile actions — coords squashed but relative structure holds.
- **Event-driven hybrid + anti-loop:** `HybridBrain` = autopilot by default, wake the LLM only at decisions; `play_loop.py` adds a **progress watchdog** (halts when no real progress for N steps — *makes mistakes but never infinite-loops*) and a **budget guard** (cap LLM calls). The autopilot is inherently loop-free (visited-map + frontiers); the watchdog bounds the LLM.
- **Mode detection (Step 3b, free):** `detect_mode()` (overworld/menu/dialog/battle). *Methodology learning:* **look at the data before coding** — capturing + inspecting real frames revealed a trivial, reliable signal: **Gen-1 UI panels are pure-white (≥230) and the game world almost never is** (overworld ~0% near-white; START-menu panel 66%), so a near-white-by-region check separates modes for ~free on CPU. General principle: *overlay/UI vs world usually separates on one simple invariant — if you find it, you skip the ML.* Non-overworld now auto-wakes the LLM (HybridBrain). overworld/menu confirmed on pixels; battle/dialog are priors.
- **Mode detection — VALIDATED on real pixels (Iteration 03 follow-up, free):** `eval/capture_modes.py` scripts the gated opening (bedroom→1F→Pallet→Oak cutscene→lab→**pick Charmander**→**rival battle**) with a seeded random-walk + A/B mashing, using RAM (`map_id`/`in_battle`/`party_count`) *only* as a navigation aid and scoring oracle — never fed to the perceiver. This finally produced the real battle/dialog frames that were missing, turning the battle/dialog "priors" into measured results:
  - **Real battle frames: 8/8 → `battle`.** Real Oak dialog → `dialog` (eyeballed), real `START` → `menu`.
  - **Overworld (RAM-confirmed moves): 76/78 → `overworld`** (the 2 misses are Pallet frames where the tile moved *and* a sign/cutscene textbox was on screen → read `menu`; waking there is the safe call).
  - **Headline (overworld vs NON-overworld, which is what drives HybridBrain's wake): 84/86 = 97.7%.** This is the property that matters — the exact non-overworld sub-label is secondary.
  - **Bug found + fixed by looking at the pixels:** the structural "bright top AND bottom ⇒ battle" prior also fired on full-screen **white/black fade-flash** frames (an all-white starter-cutscene flash read as `battle`). Measured signature: fades have **std 0.0**, real battles std > 65 (dark sprites/HP-bars/text on the white battlefield — battles are *mostly white too*, so "white ⇒ not battle" would be wrong). Fix: a **near-uniform-frame guard** (std < 6 ⇒ transition ⇒ overworld) — removes the fade false-positives without touching real battles. *Reinforces the Step-3b principle: look at the data before coding the rule.*
  - **Known limitation (honest):** the naming-keyboard / other full-screen bright menus still read as `battle` (std is high, region-white is high — region features can't separate them from battle). Harmless for the wake decision (both are non-overworld); firming up battle-vs-menu needs *structural* detection (HP-bar/sprite templates), deferred.
- **Gating, observed for real:** the autopilot couldn't reach a wild battle because Red **gates Route 1 behind getting the starter** (Oak blocks you). A concrete instance of the dependency/gating problem — the agent literally cannot progress without the menu/dialog capability. (Reinforces the gating-probe as the right test for class-2 reasoning.)
- **Outcome loop (feature #1, free):** `OutcomeMemory` — per (situation, action) record whether the action had an *observable effect*; repeated no-effects mark an action "dead" and it's surfaced to the planner ("these did nothing here, don't repeat"). Generalizes the wall-memory to *any* action; world-agnostic (no game knowledge). The simplest real seed of learn-from-mistakes.
- **goto(target) nav (feature #2, now whole, free):** the autopilot BFS-pathfinds to a *named cell* (reusing its frontier BFS — plain shortest-path on the unweighted grid, not A*), not just the nearest frontier. The **planner→target hookup is now built and unit-tested for $0**: a woken LLM may add a `GOTO: x y` line; `LLMButtonBrain._parse_goto` reads it *only* from that dedicated line (prose like "go to the stairs" is never mistaken for a target); `HybridBrain` **persists** the destination and hands it to the free autopilot on subsequent overworld steps (name a far target *once* → drive there for free, no per-tile LLM calls), then **clears it on arrival** (pose == target). The plugin's symbolic render now lists frontier coords so the planner has concrete cells to name. Proven with a stub planner (3 tests); the only thing still waiting on credits is a *live* LLM choosing the target. *Design note:* goto pathfinds over **visited** cells only, so today it means "go back to a known place / a frontier I can see" — its reach grows the moment perception gains semantic place labels ("the gym"), with no change to this seam. This is the concrete instance of the north-star loop's `decide→act` split: the expensive brain sets intent, the cheap controller executes it.
- **Gating probe — built + free plumbing passing (this session):** a synthetic `GateWorld` (`games/gateworld/`) that isolates **class-2 means-ends reasoning with backtracking** (fetch an item → carry it back → open a gate → reach the goal) and is built to separate **reasoning from recall** — two skins of the identical world (`familiar`: key→door prior; `novel`: fragment→barrier, no prior) under one **neutral prompt**; verdict = the solve-delta. Crucially it runs the **same agent unchanged**: it speaks the GB button contract and emits the same role-named `SymbolicState`, so `HybridBrain(ExploreBrain, reasoner)`, the runner, and the gateway need zero edits. Free result (scripted *oracle* reasoner = plumbing check + upper bound): **both skins solved in 27 decisions, 6 reasoner wakes** (autopilot handled 21/27 free); `test_autopilot_alone_cannot_open_the_gate` confirms exploration alone never opens it, so class-2 reasoning is genuinely required. +10 tests (67 total). The real reasoning-vs-recall measurement (`--brain llm`) is the only credit-gated step. Spec: `reports/2026-06-15-gating-probe-spec.md`.
  - *Fidelity lesson:* a new world must honour the **motion contract** the controller assumes — `ExploreBrain`'s `[d,d]` means "turn, then move" (net 1 tile); a naive move-per-press world made it overshoot and **oscillate forever**. Matching Gen-1 turn-then-move fixed it.
  - *Finding the probe surfaced (generalization TODO):* the outcome loop's `state_signature` is **pose-centric** — picking up the item changes inventory, not pose, so the loop mis-records the `A` press as "no effect" and could mark it "dead" right when it's needed. Friction, not a blocker (the text still says "you picked up the …"), but the effectiveness signal should include inventory/state deltas.
- **Honest blocker still:** LLM-side validation (battle decisions, the live `goto` target, the live Brock slice, and the live gating-probe verdict) waits on Anthropic credits. Everything above is built + unit-tested for $0 and ready.
- **Live run #1 (2026-06-15, credits added) — stuck in the house, ~$3 wasted; full post-mortem in `reports/2026-06-15-live-run-01-postmortem.md`.** First real LLM-in-the-loop run (hybrid + aria/Haiku, 400 steps): **never left the start house** (38↔37 only), 0 progress. Root cause (data-confirmed): **interior stair-warps are low-diff (~13–29) so the perceiver never detected them as area changes** (0/25 cleared `area_threshold=60`) and **never reset its coordinate frame** (`area`=0 all 400 steps). It dead-reckoned ONE drifting map across both floors → a **permanent unreachable phantom frontier at (0,0)** → autopilot "stuck" every step from #49 → **LLM woken 351/400 (88%)**, flailing toward an impossible target (it even narrated the loop). *Crisp lesson:* **instantaneous perception is solved (399/399 walkability, modes 100%, no confabulation) — the bottleneck moved to spatial-memory integration across transitions.** *Cost lesson:* `play_pokemon.py` has **no progress watchdog** (play_loop does → would've halted ~step 60), the anti-loop guarantee has a hole (an *unreachable* frontier ≠ "no frontier"), and **prompt caching was off** (`cached_tokens=0`, 2.63M input tokens uncached). Top fixes: watchdog + frontier-abandonment + caching (cheap), then fade-based transition detection + a place-graph (the real fix).
- **Live run #2 (2026-06-15, recorded, clean-start, guarded) — SUCCESS + a new wall; report `reports/2026-06-15-live-run-02.md`.** With the seam/portal fix + guardrails, the agent **reached Oak's Lab** (oracle maps 38→37→0→40, 57 cells) for **~$0.23** (30 bounded wakes vs run #1's 351/~$3); the free autopilot drove 76/123 steps. The run-#1 spatial failure is **solved on real hardware** — the iteration's thesis validated live. *New findings:*
  - **The LLM hallucinates its location (semantic perception gap).** Ground truth = Pallet→Oak's Lab; the model narrated "Viridian City"/"Gramps"/"Poké Mart" — places it never reached. Geometry (walkability/mode) is solved; the *semantic* read of where-it-is/what's-happening is unreliable confabulation. **Always verify against the oracle, never the model's narration.**
  - **It can't transact with a scripted gate.** Oak's forced dialog pulled it into the lab, but the LLM mashed A/B/directions without grasping "let Oak finish → choose a starter." Navigate ✓, comprehend/complete ✗. Gates are also **wake-expensive** (24.4% wake rate, ~4× the free 5.7% estimate, almost all in the dialog burst).
  - **Learning loop inert — ROOT CAUSE (corrected after grounding in aria's code):** the lesson channel is **LIVE, but muzzled by our prompt.** aria parses `<lesson>…</lesson>` from every reply and appends to `lessons.md` at runtime (`aria/src/aria/memory.py:245`; advertised in `prompt.py:182`) — but our `POKEMON_SYSTEM` says "reply EXACTLY THINK/MOVE, nothing else" + `max_tokens=64`, so the model never emits a tag. Proof it's the prompt not aria: the *episodic* layer fired (`earlier_today.json` captured the failure verbatim), only the *procedural* layer was starved. *(Earlier I wrote "no channel exists" — that was imprecise; the harness `_parse` is irrelevant because aria strips lesson tags before the harness sees the reply.)* Fix, per the learning-boundary law: a **harness-owned `LESSON:` line** (plain line aria passes through, NOT `<lesson>` → that persists to `lessons.md` across runs) captured into a per-run buffer, re-injected within the run, discarded at end. Needs the muzzle + token cap lifted.
  - *Cost mechanics:* aria bills ~1.8 LLM calls per agent-decision (its pipeline), ~3.3K constitution tokens injected **uncached** every call (`cached_tokens=0`). The win came from *fewer wakes*, not cheaper calls — caching still helps at higher wake volume but isn't blocking.
  - *Process win:* clean-start now **archives before wiping** (`reset_aria_memory.py` → `aria_memory_archives/iter-NNN_<date>.zip`); the wipe verified clean by the prompt dropping 6,128→3,257 tokens. Recorded MP4 (`runs/run2.mp4`, video+audio) for review.
- **Step 1 — un-muzzle lessons into a HARNESS-owned per-run buffer (free, branch `feat/lesson-buffer`, commit `45271c4`, not pushed).** Fixes run #2's inert learning loop *within the learning-boundary law*. `POKEMON_SYSTEM` drops the "EXACTLY … nothing else" muzzle and advertises an optional `LESSON:` line; `_openai_complete` `max_tokens` 64→128. `LLMButtonBrain` parses the line (`_parse_lesson`, a sibling of `_parse_goto` — read only from a dedicated line), keeps it in a **per-run buffer** (`self.lessons`, cap 8, whole-buffer case-insensitive dedup), re-injects it on later wakes, and discards it when the object dies at run end. Deliberately a plain `LESSON:` line, **not** aria's `<lesson>` tag (which would persist to `lessons.md` across runs — forbidden). 90 tests (+14). *Process learning — adversarial review earns its keep:* a 5-dimension review-then-refute workflow surfaced three real issues I'd missed: (a) the `_parse` fallback still leaked direction words from *untagged* prose and a same-line `MOVE: x LESSON: …` produced a **spurious extra button** — fixed by trusting the MOVE line exclusively (truncate concatenated directives; only scan prose-minus-tags in the no-MOVE-line free-text path); (b) `self.goto`/`self.lesson` went **stale on a model-call exception** (a latent pre-existing `goto` bug) — fixed by resetting both at `decide()` start; (c) dedup checked only the last entry — widened to whole-buffer case-insensitive. *The review also over-fired:* ~10 of 18 "confirmed" were confirmations-of-correctness or theoretical nits; judgment still required on top of the verdicts.
- **Step 2 — disconfirm/surprise detector, the "act → observe → learn" spine (free, commit `7ae55ad`).** New `core/disconfirm.py` `DisconfirmDetector`: counts consecutive decisions with no observable change (`sig` delta) and, at a threshold, emits ONE `SURPRISE: …` note that asks for a `LESSON:` (captured by step-1's buffer); consuming it resets the streak so it nudges periodically, not every step. It enriches the note with the SymbolicState's `last_action.outcome` (`blocked`/`changed nothing`) — *the disconfirm signal was already a role in the perception schema*, so the detector just reads it. **It replaced the old inline loop-breaker** (which counted only autopilot-*stuck* wakes and reset on any mode-wake). *Key insight that justified the refactor:* the old breaker **could not catch run #2's dialog flail** — Oak's forced dialog is a *mode-wake* every step, never "stuck", so no nudge ever fired; the new no-progress signal grows on mode-wakes too and finally asks for a lesson. World-agnostic, harness-owned, discarded at run end. *Review learning:* the adversarial pass found **no code bugs** this time but three real **test-coverage gaps** (streak must survive free autopilot steps; must reset on real progress; the note must reach only the wake, never the free autopilot's context) — proof that "tests pass" ≠ "the new behavior is locked"; the highest-value tests are the ones that pin the *difference* from the thing you replaced. 101 tests (+11 over step 1).
- **Step 3a — fail-safe dialog auto-advance (free, commit `facd598`).** Mash the confirm button through a PLAIN textbox for free, waking the LLM only at a real choice — run #2's wake-burst was almost all Oak's forced dialog. *Data-first paid off again (`eval/capture_dialog.py`):* captured real START-menu/YES-NO/keyboard/dialog frames and found the trap — a YES/NO box sits in the **upper-right OVER the textbox**, and `detect_mode` calls that "dialog" (the bottom textbox dominates), so auto-advancing on the mode label alone would **mash the choice**. Measured near-white in that upper-right region cleanly separates it (plain dialog ~0.00, empty box ~0.08, YES/NO ~0.33, START menu ~0.94 → threshold 0.15). Fix: `detect_mode` relabels a textbox-with-upper-right-box as "menu" (→ wake); `HybridBrain(advance_on_dialog=True)` presses A through plain dialog free and resets the disconfirm streak (advancing IS progress the signature can't see). Validated on 272 real frames; 53 became free auto-advances. The fail-safe direction: a false "choice" costs one wake, never a mashed choice.
- **Step 3b — Gen-1 textbox font decoder, DONE (free, commits `279dd9e` + review-hardening `a3e6dcd`).** Reads textbox text from PIXELS (no RAM/VRAM) for (i) a missed-text transcript injected at the next wake and (ii) semantic grounding — the LLM now reads its actual location instead of hallucinating (run #2's headline failure). *The grid is the standard Gen-1 layout* — 2 lines at y=112/128, 8×8 cells from x=8 — and 8×8 template-matching against a 42-glyph asset (`gen1_font.json`) decodes **all 6 calibration frames AND a held-out frame at 100%**. *Decisions grounded in data:* (a) calibrate from the **dialog grid itself**, NOT the naming keyboard (its charset is on an irregular ~17px pitch → pixel-offset mismatches); (b) the blinking ▼ "more" arrow is at the FIXED bottom-right cell (17), learned → "" (dropped); (c) a **quality guard** (≥3 known chars) makes a non-textbox screen yield "" not junk; (d) unknown glyphs → '?', never a wrong guess. Wired: perceiver → `SymbolicState.screen_text` → `obs.text` + `HybridBrain` per-run transcript. *Review learning:* the step-3 adversarial pass found **no bugs** — the one "major" was a real but theoretical safety gap (a choice box sitting LOW could be missed and auto-advanced); fixed by widening the choice-detection region to just above the textbox (validated it keeps plain dialog at 0.0). *Process learning that repeated all session:* "look at the data before coding" + "verify before claiming" kept paying off — I paused 3b mid-build rather than ship a hastily-labeled (=wrong-text) table, then completed it once the calibration was validated.
- **Live run #3 (2026-06-16, recorded, clean-start, guarded) — the run-#2 wall is BROKEN; report `reports/2026-06-16-live-run-03.md`.** With steps 1–3 live, the agent **comprehended Oak's forced gate, chose SQUIRTLE, and reached the rival battle** (vs BULBASAUR) — maps 38→37→0→40, 87 cells, for **~$0.33** (40 wakes, budget-capped). Run #2 never got the starter; run #3 did. *Why it worked, mechanism-confirmed:* **dialog auto-advance handled 123 dialog frames for FREE** — the gate that burned run #2's entire 30-wake budget cost ~nothing — and the LLM was woken only at the **5 real choices** (examine ball / choose starter / nickname YES-NO) + the battle. The **textbox decoder grounded it live**: decoded straight from run-#3 frames, *"ASH received a SQUIRTLE!"* and *"ASH! Let's check out"* (uppercase gaps read as '?', never a wrong guess) — the run-#2 location hallucination is gone because the LLM read the actual events. Caching even **partly engaged** (96K cached tokens vs run #2's 0). It halted **mid-rival-battle** on the budget cap — the honest edge of our capability. *New bottleneck:* **battle-move decisions** (no FIGHT/move policy yet). *Honest gaps surfaced:* the oracle doesn't log `screen_text`/`LESSON:`s (so live grounding/lesson *content* isn't auditable from `oracle.jsonl` — add it); the decoder's uppercase coverage is partial (extend via `calibrate_font.py`). *Process win:* clean-start (archived iter-002) + guardrails + record + RAM-oracle verification held; both RAM truth (`in_battle`, map ids) and a decoded frame confirm the starter+battle, and the budget cap stopped the spend at the capability edge.
- **Post-run aria-memory audit (run #3, free — settled a question without a paid re-run).** Reading aria's regenerated memory after run #3 answered "did the harness `LESSON:` buffer (step 1) actually engage?" *for free.* The run's **journal proves it did**: the model emitted **`LESSON:` 56×** and **`<lesson>` 0×** (it used OUR channel, not aria's), and a captured prompt shows BOTH the re-injection (`"LESSONS you recorded earlier THIS run (apply them):"`) and the decoded transcript (`"Text shown since your last decision (auto-advanced): ?ARY received"`). *Honest correction:* my in-the-moment worry that aria's `<lesson>` channel pre-empted ours was **wrong** — verify-before-claiming cuts both ways, and the cheap free check beat a paid re-run. *Law check:* aria's reflection separately appended 4 lessons to its durable `lessons.md`/`core_memory.md` during the run, BUT the committed seed has **0** dated lessons and `reset_aria_memory.py` git-reverts them → **no cross-run leak; the law holds** (as long as reset runs, which is mandatory). **The real new finding — a belief-update gap:** aria's reply narrates *"Bulbasaur received"* while ground truth is **Squirtle** (it actually got Squirtle; the rival has Bulbasaur). Even though the decoder fed it *"Got Squirtle!"*, it **kept its prior intention and mis-attributed the contradicting evidence** instead of updating. So the decoder fixed *where/what am I reading* but NOT *let an observation overturn what I already decided* — exactly agnostic-feature #4 (observation-grounded belief check). Follow-up shipped: `screen_text` is now logged to the oracle (`e546011`) so future runs are auditable without journal-spelunking.
- **Two diagnoses from the post-run review (frame the next phases).** *(a) The textbox decoder isn't "unreliable" — it's UNDER-CALIBRATED.* A Gen-1 glyph that's IN `gen1_font.json` decodes 100% exactly (a fixed 8×8 tile font, identical bitmap every render); the `?`s are honest "uncalibrated glyph" (mostly uppercase), never wrong guesses. So template-matching is the RIGHT tool, not a weakness — for a tiny fixed bitmap font it's exact/instant/CPU-only/zero-dep, whereas off-the-shelf OCR (Tesseract/cloud) is *worse* (built for anti-aliased photographic text, needs upscaling, still errs on 8px glyphs) and adds cost/latency/deps. The full-reliability fix is **ROM font extraction** (build-time asset of all ~100 glyphs → 0 `?`; runtime stays pixel-only), not OCR. OCR earns its keep only later, for cross-*game* generalization where per-font calibration is a burden. *(b) "It still walks repeatedly" — diagnosed against run #3's oracle:* genuine oscillation is now RARE (**2** A→B→A events; the seam/portal fix mostly killed run-#1 looping). The alarming revisit counts (a tile ×59/×54) are the agent **standing still through ~123 dialog + 16 battle frames** (x,y fixed during dialog/battle) — NOT re-walking. The *real* residual re-treading (a Pallet tile ×29) is **dead-reckoning drift**: one tile per move squashes the geometry, so the occupancy map's visited/frontier model degrades AND the pose-only `state_signature` blunts the disconfirm loop-breaker (drifting poses hide "no progress"). Fix = a drift-robust **topological place-graph** (Phase B), not more dead-reckoning. *Lesson:* check the oracle before believing the video — "it's looping" was mostly "it's standing in a long cutscene."
- **Phase A items 1+2 — battle-move policy + belief re-grounding (free, harness-only, 2026-06-16; uncommitted; 133 tests, +14).** Run #3 halted inside the rival battle, so the plan was "teach it to fight." The build surfaced several non-obvious things:
  - **The battle "policy" was mostly a PACING bug, not a reasoning gap.** Run #3's ~40 wasted battle wakes weren't bad move choices — the agent was being woken on **mid-animation frames that never showed a menu** (the fixed 16-frame `press` settle lands mid-animation; battle animations run 100+ frames). The fix is *temporal* (advance until the screen is static), not a smarter prompt. *Data-first again:* I first proved **brightness/region features can't separate battle sub-states** (intro vs action-menu vs move-list are all ~0.8 near-white) — so a structural detector was out, and a settle-until-still was the right tool.
  - **Single-step frame-diffs are too noisy to mean "settled."** Mid-animation frames dip transiently below a naive threshold (measured lows like 0.73 / 0.88 between busier frames), so a one-shot diff would declare "static" early. Reliable "waiting for input" = a **sustained** static window: `window`=24 consecutive sub-`eps` diffs. Calibration from real battle frames: animation diffs 13–216, true-static < 3, and a **blinking cursor ~0.7 mean** — so `eps`=2.0 tolerates the blink (it's not "still", but it's "waiting") while `window` rejects the transient lows. The blinking cursor is exactly the adversary a naive threshold trips on; `advance_until_static` is kept **pure** (frames in, verdict out) so this logic is unit-tested with synthetic sequences.
  - **The pose-only `state_signature` breaks in battle — the THIRD venue for the same flaw.** A frozen signature (battle menu cursor ≠ world pose) makes `OutcomeMemory` about to mark **A** — the one button you need to fight — a dead/"avoid" action, and fires a spurious `SURPRISE:` every few turns. Fix shape is identical to the auto-advanced-dialog case: when `context=="battle"`, **don't tally and clear the streak**. This is now the third time pose-only signatures have bitten (item pickup → dialog flail → battle), each one reconfirming the Tier-2 finding *track progress globally, not per-(pose,action)*. (`screen_text` deliberately stays OUT of the signature — a test pins that, because dialog-flail detection needs a constant sig.)
  - **Belief-update could NOT be a harness text-matcher — push the gate to the layer that can perceive.** The obvious design (fire `SURPRISE: screen says X, you said Y` when the stated belief contradicts `screen_text`) is **structurally impossible**: the decoder mangles the uppercase Pokémon names it would have to compare (`SQUIRTLE`→`?O??RT?E`). The capability that *can* read those names is the **model's own vision**, so the right intervention is a prompt nudge (`TRUST THE SCREEN`, gated on `screen_text` being present so plain overworld wakes stay lean) rather than harness logic. *General lesson:* when the harness can't perceive the thing it would gate on, move the gate to the layer that can — don't build a detector that's blind to its own trigger.
  - **No-leak held for pacing too.** The settle is gated by `detect_mode(...)=="battle"` (**pixels**), never RAM — pacing decisions act from the screen like perception does.
  - **Validated free, no brain.** `eval/verify_battle_settle.py` drives PyBoy with RAM-guided nav + the *production* `PyBoyEmulator.settle`: every settle returned True, a ~116-frame send-out animation **collapsed into ONE observation**, and the action-menu + move-select were reached in **~10 settled observations** (vs run #3's 40 that never got there). So the paid run #4 only has to test the *decisions*, not the plumbing.
  - **Process honesty — the review was partial, then completed clean.** The first 5-dimension review→refute workflow returned **0 confirmed issues** but lost 2 dimensions (signature-fix, test-coverage) to session limits. Rather than claim a clean review, I flagged the gap and **re-ran both** once limits reset. Signature-fix: 0 bugs (the reviewer empirically checked that `detect_mode=='battle'` covers all 46 captured battle sub-screens and 0/348 non-battle frames mislabel — so the "menu vs battle" worry I'd have hand-waved was actually *verified*; the one real note is the intended total suppression of in-battle SURPRISE, backstopped by the watchdog/budget). Test-coverage: confirmed (by *actually reverting* each part) that all 4 change-parts are pinned by a revert-failing test, and surfaced cheap boundary gaps in the pure `advance_until_static` helper — the lesson again being *the highest-value test pins the exact stopping threshold the feature rests on*. Closed with **+5 hardening tests** (`diff==eps` strict, None-frame-mid-stream, exact-window; belief-nudge whitespace + coexistence) → 133. *Meta-lesson:* a reviewer that verifies a claim empirically (run detect_mode over every frame on disk; revert and watch the test fail) is worth far more than one that reasons about it — and it's cheap.
> _Runs #4–#12 also have a consolidated standalone write-up with a dedicated, oracle-verified section
> per run: **`reports/2026-06-20-live-runs-04-12-digest.md`**. The entries below are the original
> chronological log._

- **Live run #4 (2026-06-17, recorded, clean-start, guarded) — never reached the battle; the bottleneck is navigation, not fighting. Pivoting to Phase B.** With Phase A committed (settle + signature fix + belief nudge) the bar was "get *through* the rival battle." Instead the agent **never got the starter**: oracle trajectory `38→37→0→39` — it entered **map 39 (the rival's house), not map 40 (Oak's lab)**, then wandered Pallet Town (270 of 398 steps) and the **watchdog halted it** (no map/cell/level/badge progress for 120 steps). 14 wakes / 398 steps (3.5%), **~$0.11** — the guardrails worked perfectly: a stuck run was stopped cheaply, nowhere near the $0.83 cap. *Honest reading:* the Phase A battle policy is **unexercised, not refuted** — the failure is entirely upstream, at the **unreliable lab-entrance navigation** (run #2 also failed to get the starter here; run #3 reaching the battle was partly luck). Not a Phase A regression: settle is battle-only (never fires in overworld, pinned by test), the signature else-branch is unchanged, and the one always-on addition (the `TRUST THE SCREEN` nudge) fires on `screen_text` which is ~empty in plain overworld; the evidence points to the known **dead-reckoning drift / occupancy-map degradation** (the pose-only model can't reliably pinpoint the single lab-door tile). *Decision:* **reprioritize Phase B (topological place-graph + fade-based transition detection) BEFORE re-testing the battle** — reaching the battle reliably is the precondition for testing how it fights. *Method note for the battle re-test later:* isolate it with a pre-positioned rival-battle `.state` fixture (RAM sets up the fixture; the agent still acts from pixels) so flaky overworld nav doesn't gate the battle-policy test. *Guardrail lesson reconfirmed:* the watchdog's RAM-oracle progress fingerprint freezes during long cutscenes/battles AND during a stuck overworld loop — here it correctly caught the stuck loop; the same property is why a real battle needs a generous `--stuck-steps` (or the isolated fixture).
- **Phase B — translation odometry + topological place-graph (the navigation fix, free, harness-only, 2026-06-17; 139 tests).** Run #4 stuck at the lab because the frame-diff area detector missed 8/10 warps and LUMPED distinct maps into one corrupt occupancy area. Phase B rebuilt the area model. The arc — and *what caught what* — is the lesson:
  - **The transition signal is ego-motion vs scene-cut (David's idea, data-validated).** Within a map the camera scrolls a centered player, so frame N+1 is frame N TRANSLATED — some integer-tile shift aligns them (`_best_shift`, min overlap mean-abs-diff over ±4 tiles). Across a warp NO shift aligns. Measured (`eval/inspect_translation`): same-map best-shift diff p90 **5.4**, real warps **55–77** — and it catches interior **STAIRS** (55), which the fade signal misses (stairs don't fade). *Why better than the fade I'd built first:* works on the frames the perceiver already has (no emulator hook), and the shift magnitude is true odometry.
  - **The FADE is kept as a backstop** (`_is_fade`, std<6, watched intra-press in the emulator → `context["transition"]`): it fires even right after a menu, where translation has no valid `prev` to subtract — run #4's *dominant* miss. So: translation primary (scene cuts incl. stairs), fade for the post-menu/degenerate cases. Plus a `detect_mode` fix — a bright outdoor scene (Pallet roofs/paths, bottom near-white ~0.16) no longer falls through the catch-all to "menu" (that false-positive was triggering the resync that masked the next warp).
  - **Topological place-graph:** a warp crosses to a persistent PLACE; `_transit` reuses a KNOWN place (restoring its accumulated map) via a **direction-independent door edge** (you enter a building one way, leave the doormat another — so key by CELL, not direction) + a reverse edge, else mints a new one. A building round-trip returns to the same Pallet map instead of re-exploring it.
  - **The headline process lesson — the closed-loop autopilot test caught two bugs every unit test passed through.** "138 tests green" did NOT mean it worked: (1) **measured odometry broke the ExploreBrain's `[d,d]`=net-one-tile motion contract** — recording the true 1-or-2 tiles made the controller overshoot/oscillate; it sat stuck in the bedroom. Fix: keep the shift for robust moved-vs-blocked detection but **cap the cursor advance at 1 tile** (the full drift fix now waits on a controller that understands variable steps — DEFERRED). (2) **A house-door ping-pong** (~98 flips): the re-baseline-after-warp I added to kill a post-warp false-positive also *missed the immediate return* → map-lumping; removing it instead let a post-warp FP at the entry cell hit the reverse edge → lumping the other way. The fix that converged: **re-baseline the TRANSLATION one frame (fade still fires through it) + seal BOTH door cells** (source + the cell behind the arrival; arrival stays explorable so the frontier-only autopilot isn't stranded). Closed-loop result: autopilot cleanly traverses `38→37→0`, **0 lumping, 0 ping-pong**. *Takeaway: a frontier-only controller + a "more accurate" map can be worse than a consistent-but-squashed one; validate the loop, not just the primitives.*
- **Live run #5 (2026-06-17, recorded, clean-start, guarded) — Phase B navigation VALIDATED; lab completion blocked because aria RAN OUT OF ANTHROPIC CREDITS mid-run.** With the clean map the agent **reached map 40 (Oak's lab)** — `38→37→0→40`, 241 steps in the lab — **past run #4's Pallet wall**; perception held live (**0 ping-pong**, 1 minor lump {0,40}). It worked for ~45 wakes (reaching the lab), then aria/litellm 400'd the remaining ~55 → no starter, budget-cap halt. **The error is BILLING, not context** (litellm container log): *"Your credit balance is too low to access the Anthropic API."* — and NOT the harness (the transcript is capped + reset per wake, `brains.py:225,249`). **Honest correction:** in the moment I diagnosed this as an "aria context ceiling (~45 wakes)" from the wake count alone and even committed that into the docs — *wrong*. Reading the litellm log (run #6, below) showed the truth was credit exhaustion. *Verify-before-claiming applies to your own diagnoses: a wake-count correlation is not a root cause — read the actual error.* The credits ran dry ~45 wakes into run #5 (the cumulative spend across runs hit zero there).
- **Live run #6 (2026-06-17) — isolated battle test; confirmed the credit exhaustion; battle policy still UNTESTED.** Built `eval/make_battle_state.py` → `rival_battle.state`, a fixture parked at the rival battle (RAM-guided setup; in_battle, map 40, starter in party, `detect_mode=="battle"`), so Phase A's battle policy could be tested in a SHORT run sidestepping the long nav. But **all 30 wakes 400'd from the first** — zero credit balance left after run #5 — so the battle policy was never exercised. This both (a) confirmed the run-#5 cause is credits (not context: a fresh, tiny-context battle observation still 400'd immediately), and (b) leaves the battle policy validated only by the free `eval/verify_battle_settle.py`, not live. **Blocker: top up the Anthropic account behind aria**, then re-run from the fixture. (This is the same credit-exhaustion blocker noted 2026-06-14; it recurred.)
- **Live run #6b (2026-06-17, credits restored) — battle policy MECHANICALLY VALIDATED live; bottleneck moved to move-selection + belief-grounding.** Re-ran from `rival_battle.state` (cap 30 wakes, **0 errors**). **The reach+settle+act machinery works:** the agent stayed in the fight every turn on stable battle screens, recognized *"Gary wants to fight → select FIGHT"*, advanced text — the first LIVE proof of Phase A item 1 (runs #3–5 never got a testable battle). **But it did NOT win** (`in_battle` stayed 2 all 30 wakes), for two reasons the decoded text exposed: (1) **it mashed `A` every turn** → alternated SCRATCH (attack) and GROWL (a non-damaging stat move — decoded `Enemy SQUIRTLE's ATTACK fell!`), so half its turns did no damage, at a type DISADVANTAGE (it has **CHARMANDER** vs Gary's **SQUIRTLE**, fire<water) → 30 wakes didn't finish. *"First move is a fine default" does NOT guarantee an ATTACKING move.* (2) **The confabulation / belief-update gap (agnostic-feature #4) persists** — the agent narrated having **Bulbasaur** and *"defeating Squirtle"*, while the decoded screen (`Go! CHARMANDER!`, `Enemy SQUIRTLE`) + RAM say it has Charmander and the fight is ongoing. The `TRUST THE SCREEN` nudge was injected (screen_text present) but did NOT override the confab — it's too weak. *Takeaway: the battle MECHANISM is solved (reach/settle/act); battle STRATEGY (deliberately pick a damaging move, not mash A) and belief-grounding are the next constraints — exactly what a probe should surface. Cost ~$0.25, budget-cap halt.*
- **Battle policy v2 + runs #7/#8 (2026-06-17) — the prompt fixes did NOT close the gap; the confabulation is a confident, INVERTED world-model a nudge can't dislodge (a north-star finding).** v2 (harness-only): `POKEMON_SYSTEM` told it to read the screen, name its own Pokémon + the foe, and pick a DAMAGING move (not mash A into GROWL); the `core/` belief nudge was strengthened to "state what you see now; the screen wins; don't narrate the old version." Re-tested from `rival_battle.state`. **Verdict: it didn't work.** Run #8 (clean, cap 50, 0 errors): the agent reasons *"Water beats Fire — I'll use WATER GUN to finish Charmander"* — but the decoded screen says **its own Pokémon is CHARMANDER and the foe is SQUIRTLE**. It has the identities **fully inverted** (believes IT is Squirtle/water, the enemy is Charmander) and even reads its own Charmander's GROWL as the enemy's. It still **mashes A** (every move `-> a`), so the game uses Charmander's real moves (SCRATCH+GROWL mixed) under a WATER-GUN fantasy, and it did **not** win (in_battle=2 at the cap). *The deeper lesson:* belief-grounding is **harder than a prompt** — the model builds an internally-consistent WRONG model ("rival battle → I should have the type advantage → I'm the water type") and reasons confidently from it; "trust the screen" doesn't overturn a coherent prior. Move selection is downstream of the confab (it mashes A executing the fantasy). *Concrete next lever (not taken — paused here):* the decoder already reads `Go! CHARMANDER!` / `Enemy SQUIRTLE`, so the HARNESS could parse those and inject a hard fact ("your active Pokémon is CHARMANDER; the foe is SQUIRTLE") instead of a soft nudge — a Pokémon-specific grounding stronger than the agnostic one.
  - *Process honesty — I over-claimed twice on this run and the data corrected me.* From run #7's CRASH-salvaged 39 steps I saw all-SCRATCH and said "move-selection validated" — a lucky highlighted-move streak, not a fix (run #8 mixed GROWL back in). And `any(in_battle==0)` flagged a "win" that was a **1-step RAM read glitch** mid-battle (step 41), not a real clear. Both caught by reading the per-step truth + the actual reasoning text. *Verify-before-claiming applies to my OWN preliminary analysis, not just the agent's narration — a single-frame oracle blip and a word-count both lied.* (Also: run #7 hard-crashed at step 39 with no traceback because stdout was buffered; re-running with `python -u` captured a clean run — buffer your paid runs unbuffered so a crash leaves evidence.)
- **Run #9 (--no-vision) — the confabulation source is the BATTLE IMAGE (a clean A/B test), and a stabler OCR is the cure.** Hypothesis (David's): is the image acting as noise? Ran run #8 with the image OFF, everything else identical. **The confident inverted confab VANISHED:** the agent said CHARMANDER ×19 (correctly, from the decoded text) and **0** "WATER GUN"/"Squirtle"/"water" — vs run #8 (image on) where it confidently believed it was Squirtle. So Haiku, reading the low-res battle SPRITES, was fabricating the wrong identity and reasoning from it (the founding Iteration-01 weakness — "Haiku confabulates from raw pixels" — resurfacing in the one place we never decoded: the battle). **But text-only isn't usable either:** the agent kept saying *"I cannot see the screenshot you've attached"* (the prompt still references an image in `--no-vision` mode — a harness bug) and the decoded enemy name was too garbled (`?O??RT?E`) to act on, so it just pressed A. *Conclusion: neither raw-image (confabulates) nor raw-text (garbled) works — the cure is CLEAN DECODED TEXT.* So the principled battle fix is the same move the project made for navigation: decode the state, don't make the LLM read pixels.
- **OCR: the decoder isn't unstable, it was incomplete — and the "fuzzy" fallback was the real instability.** *Reframe:* exact 8×8 bitmap-matching is the MOST stable technique for a fixed tile font (a calibrated glyph matches 100%); off-the-shelf OCR (Tesseract) is worse on 8px glyphs. The `?O??RT?E` was missing glyphs (battle uppercase never calibrated — the table was built from lowercase-heavy dialog). *The genuine stability bug found while fixing it:* the decoder's Hamming-distance fallback (`tol=4`, meant to absorb an edge pixel) **misreads an uncalibrated glyph as a confident WRONG char** — Squirtle's `Q` decoded as `O`, not `?`. So "instability" = confident misreads, not flaky matches; decoding EXACT-only (`tol=0`) turns them back into honest `?` (a runtime stability lever: lower `tol`). *The robust completion (`eval/calibrate_battle.py`, no ROM):* auto-calibrate from self-verified known battle WORDS — match a word (SQUIRTLE, GROWL, TACKLE, FIGHT, …) at every cell offset and accept ONLY where ≥2 already-known cells agree (an anchor), so a misaligned/spurious match can't corrupt the table. Result: **+7 glyphs (G F I L Q S U), 0 conflicts**, and `Enemy SQUIRTLE used TACKLE` / `FIGHT…ITEM RUN` / `GROWL` now decode clean (49 glyphs, 139 tests). *Next test:* a no-vision battle with the NOW-clean decoded names (+ stop the prompt referencing a screenshot in text-only mode) — does the agent finally ground and fight correctly?
- **Run #10 (text-only + clean OCR + no-image banner) — THE CONFABULATION IS FIXED; the battle bottleneck narrowed from "wrong world" to "can't operate the move menu."** Combined all three diagnosis-driven fixes (image OFF = no sprite-confab source; completed OCR = readable names; a TEXT-ONLY banner so it stops asking for the screenshot) and re-ran from the fixture. **The grounding is now CORRECT** — the agent's own words: *"I'm sending out **Charmander** against Gary's **Squirtle** (bad matchup — Water beats Fire)"*, *"Charmander wasted a turn on GROWL… pick **Scratch** to damage it."* The inverted-identity confab (run #8's "I'm Squirtle with WATER GUN") is **gone** (WATER GUN: 0). This closes the loop on David's hypothesis: **the battle IMAGE was the confabulation source** (Haiku misreads low-res sprites — the founding Iteration-01 weakness, in the one place we'd never decoded), and the cure is **clean decoded STATE, not prompt-nudging** — exactly the move the project made for navigation. *Remaining gap (different + clearer):* **move EXECUTION** — it now understands the goal (navigate to a damaging move, recognizes GROWL wastes a turn) but the menu navigation still lands on GROWL ~2:1 over Scratch, because it can't tell which move is HIGHLIGHTED (the move list + cursor aren't decoded). It didn't win (still in battle at cap 50, bad matchup + GROWL waste; 0 errors; per-step verified, no transient-glitch false-win this time). *The banner worked* (1 residual complaint vs run #9's many — there's still an aria-side "screenshot attached" reference to clean up, cost 1 wake). **Next decode target: the battle move menu + cursor position** ("highlighted: GROWL (status); moves: SCRATCH, GROWL") so the agent can navigate deterministically to the damaging move — the same decode-the-state principle, applied to the move list.
- **Move-menu decode + runs #11/#12 — the agent WON the rival battle and got PAST it (first time ever); Phase A "fight" is DONE end-to-end.** Run #10's residual gap was move EXECUTION: it understood the goal but couldn't tell which move was highlighted, so it kept landing on the non-damaging GROWL. Fix (decode-the-state, again): `textbox.decode_move_menu` reads the FIGHT move list + the ▶ cursor from pixels (names at cell 5, cursor at cell 4 of the highlighted row; gated by a blank left margin to reject battle dialog + reserved action words to reject the FIGHT/ITEM/RUN action menu). Surfaced in `screen_text` as e.g. *"move menu — cursor on SCRATCH; moves: SCRATCH, GROWL"*. Validated on real frames (0 false positives over 154) + 2 synthetic tests (143).
  - **Run #11 (text-only + clean OCR + move-menu, cap 50):** move execution FIXED — the agent reasons *"Move menu open with SCRATCH highlighted; pressing A to attack"* and used **SCRATCH ×7, GROWL ×0** (vs run #10's GROWL 29 / Scratch 12). It read the decoded cursor and deliberately attacked. Didn't win only because of the wake cap (slow type-disadvantaged grind; battle wakes every step at ~5 wakes/turn → ~8-10 turns).
  - **Run #12 (cap 80) — WON.** Verified per-step: `in_battle` 2→0 **sustained** (~230 steps), maps `40→0→37→39` — it beat Gary's Squirtle with Charmander despite the type disadvantage, got the Pokédex, left the lab, explored Pallet. The full chain is validated live: **confab (image) fixed → move selection (cursor) fixed → won → progressed past the battle** (no run #3–#11 ever did). 0 errors. *Two things the win also confirmed:* (a) **battle is wake-heavy exactly as predicted** — ~68 of 73 total wakes were the battle alone (woken every step), the entire post-battle overworld cost ~5; this is what battle auto-advance targets. (b) **post-battle it hit the OLD lab-exit / Pallet navigation wall again** (`[wake:stuck]` "trapped indoors") — the residual Phase-B gap, cleanly separate from the now-solved battle.
  - *The bottleneck has genuinely moved off "fighting."* Next, in order: (1) **battle auto-advance** (wake only at the action/move menus, auto-advance battle text → ~4× cheaper battles; the static first rung of skill-compilation); (2) the **learned blind-execute battle policy** (System-2→System-1; cache/compile the LLM's menu decisions over the now-decoded state, re-wake on novelty — see `reports/INSIGHTS.md` §6); (3) tighten **lab-exit / Pallet navigation** (the residual Phase-B gap run #12 re-exposed).
- **Battle auto-advance (2026-06-20) — BUILT, harness-only, free-validated; the first cheap-first win after the rival-battle win.** Run #12 exposed the cost shape: ~68 of 73 wakes were the battle alone (woken every step). Fix = the same decode-the-state move, now applied to *cost*: split a SETTLED battle frame finer and auto-advance the parts the agent can't act on. `textbox.battle_subscreen` (pixels-only) classifies narration → `battle_text` vs the action/move menus → `battle`; the perceiver emits the finer `context` (only `perceive()` changes — `detect_mode` still returns `battle`, so `_settle_if_battle` keeps firing); `HybridBrain`'s existing dialog auto-advance branch is widened by ONE predicate to also advance `battle_text` for free. The LLM is now woken ONLY at the action menu and the move list.
  - **Safety was the whole design, and the asymmetry is the lesson.** The catastrophic error is mis-reading the MOVE menu as text → auto-A picks whatever the cursor sits on (GROWL — the run #6b/#11 failure). So the classifier is **positive-ID-for-advance, default-to-wake**: it returns `battle_text` ONLY on a positive narration read (≥4 real glyphs, no reserved action word, and — checked FIRST — `decode_move_menu` empty); every ambiguity wakes. A wasted wake costs cents; a mis-advanced move loses the fight — so the default is always *wake*.
  - **Validated on the 8 real `runs/battle` captures: a clean split** — narration (`GARY sent out SQUIRTLE!`, `Go! CHARMANDER!`) → `battle_text`; move menu (GROWL) and action menu (`FIGHT … ITEM RUN`) → `battle`. +15 synthetic tests (158 total). Two supporting pieces: a world-agnostic `_ADVANCE_FUSE=50` (force one wake after 50 consecutive free advances, so a pathological advanceable loop can't run forever free — covers dialog too) and a **battle-aware watchdog** in `play_loop.py` (suppress `--stuck-steps` while `in_battle`, since a fight shows no map/badge oracle-progress and — now that battle steps are mostly free — would otherwise trip the halt mid-fight; `in_battle` stays in the oracle, never the agent input).
  - *Residual to verify if ever captured:* a naming-keyboard frame reads as `battle` in `detect_mode`; `battle_subscreen` should default it to `battle` (wake) since its text isn't in the dialog rows — but no keyboard capture exists yet to confirm (default-to-wake makes it safe regardless).
  - **Run #13 (2026-06-20, paid, text-only hybrid from `rival_battle.state` — run #12's winning config + auto-advance) — VALIDATED LIVE, clean.** Verified per-step against the oracle: **WON** (`in_battle` 2→0 sustained at step 72; step 62 "Charmander defeated Squirtle and leveled up"), with **18 BATTLE wakes vs run #12's ~68 (~3.8× cheaper)** — each turn cost exactly 2 wakes (action menu + move menu) with ~6 narration frames auto-advanced free between them (54 free battle advances). **Move quality held: SCRATCH ×12 / GROWL ×0**, every move-menu wake reading *"SCRATCH is highlighted and deals damage — confirm"* — the text-only grounding stayed correct (Charmander vs Squirtle, 0 confab, 0 errors, aria credits held). Total **22 wakes / 400 steps (5.5%)**; post-battle nav cost only **4 wakes** over ~328 overworld steps — the autopilot drove it out of the lab and around Pallet (maps 40→0,37,38,39) for free. *Confirms the cheap-first thesis end-to-end:* decode the state finely enough and the expensive model is woken only at genuine decisions; the rest is free. Video `runs/run13.mp4`, oracle `runs/run13/`, pre-wipe archive iter-013, ~$0.15-0.2. **The 7 identical FIGHT→SCRATCH turns are now the obvious next compile target** (the learned blind-execute policy, System-2→System-1). **NEXT: (2) learned battle policy; (3) lab-exit / Pallet nav.**
- **Live run #14 (2026-06-20) — first integrated COLD-START run; nav holds end-to-end, but credits ran dry again (inconclusive downstream). Report `reports/2026-06-20-live-run-14.md`.** From `start.state` (text-only hybrid, all current capabilities), the agent left the house, crossed Pallet, and **reached Oak's lab (`38→37→0→40`) by step 130 on 15 productive wakes** — Phase B navigation holds from a COLD start (past run #4's lab-entrance wall, no lumping/ping-pong; run #5 only reached it before credits, this confirms it cold), and dialog auto-advance engaged in the lab (81 free Oak-dialog advances). **Then the Anthropic credit balance hit zero at step 276** (litellm log, verbatim: *"Your credit balance is too low"*) — all 65 subsequent wakes were 400s, the budget cap (which counts failed wakes) halted it at 80, and it never reached the starter/rival/lab-exit. **Same recurring external blocker as runs #5/#6; NOT a capability gap.** Spend ~$0.10 (only the 15 good wakes; 400s unbilled). *Two takeaways:* (a) the "where does it break next" question is STILL open — credits pre-empted the downstream; re-run once topped up. (b) **A real harness gap the outage exposed: no API-error circuit breaker** — the harness retried each credit-400, surfaced it as `[wake:stuck]`, and counted it against the wake budget, so an outage burns the whole cap on no-ops; halting after N consecutive identical API errors would fail fast and cheap. *Process win: the run-end auto-report hook (built this session) FIRED correctly — `reports/2026-06-20-live-run-14.md` auto-written with oracle facts + errors=65 all correct (first live validation).*
- **API-error circuit breaker + run #15 (2026-06-20) — credits were MASKING the real downstream wall: interior navigation.** Run #14's outage burned 65/80 wakes on credit-400 retries, so first built a **circuit breaker** (harness-only): aria echoes litellm errors as a 200 with the error AS the message content (they don't raise), so the brain detects error-shaped replies (`_looks_like_api_error`) AND exceptions, counts consecutive failures (`LLMButtonBrain.consec_api_errors`, reset on a real reply), `HybridBrain` forwards it, and both drivers halt at `API_ERROR_CIRCUIT_BREAKER=4` with the actual error (fail fast + loud; +4 tests, 167). Then, **credits topped up**, re-ran the end-to-end test (run #15, report `reports/2026-06-20-live-run-15.md`). **Result — the conclusive answer:** with credits healthy (0 errors, breaker correctly SILENT — first live proof it doesn't interfere), the agent reached Oak's lab cold (`38→37→0→40` by step 130, third time) but then got **wall-locked navigating the lab INTERIOR to reach Oak** — **all 100 wakes were `[wake:stuck]`**, never got the starter, never reached the battle; budget-cap halt (~$0.6-0.8). **The pose-only occupancy map drifts/corrupts in the tight lab room** — the agent physically wanders (real RAM movement) but its internal frontier model is wrong; it self-diagnoses *"the wall map is corrupt,"* and confabulates having reached Oak (pose/belief desync). **This is the residual Phase-B gap — the DEFERRED dead-reckoning/odometry drift — now the #1 blocker:** the place-graph fixed BETWEEN-map nav, but WITHIN-room nav is still pose-only and drifts, and it blocks the starter → rival → Route 1. *Also: the stuck-watchdog was placated by aimless-but-real wandering (RAM coverage ticks as it physically moves), so the budget cap (not the watchdog) stopped it — real movement ≠ real progress.* **NEXT = fix interior/short-range navigation** (the learned battle policy queues BEHIND it: the battle's already solved, but you can't reach one cold until you can get the starter).
- **Interior-navigation DRIFT fix (2026-06-20, harness-only, free-validated) — the run-#15 wall is broken; the cause was a wrong MOVEMENT MODEL, not "odometry is hard."** Measured the drift *directly* against the RAM oracle (run #15 logged both the perceiver pose AND ground-truth x/y): **40.2% of overworld moves mismatched, and 139/144 were the exact "RAM moved 2 tiles, perceiver recorded 1" case.** Root cause: the autopilot presses `[d,d]`, and the codebase assumed GateWorld's *"turn, then move = net 1 tile"* semantics — but the **real emulator absorbs the turn within the held press**, so `[d,d]` moves **TWO** tiles when open while the perceiver capped the cursor at one → ~1 tile lost per same-direction step → the interior occupancy map corrupts (the "wall map is corrupt" run #15 self-diagnosed). **Verified the true mechanics on the live emulator (`eval/probe_step.py`, free): a SINGLE `[d]` press = exactly one tile, even on a direction change (9/9 open moves, turn is free); `[d,d]` = two tiles.** Fix, two halves: (1) **`ExploreBrain` single_step** — the Pokémon drivers press `[d]` (one tile/decision) so every autopilot move stays synced with the dead-reckoned cursor; the **agnostic default stays `[d,d]`** so GateWorld's motion contract is untouched (the step granularity is a per-world property the driver injects, `core/` stays world-agnostic). (2) **measured-distance odometry** in the perceiver — advance the cursor by the best-shift magnitude (clamped to the ±4-tile search window), marking every traversed cell visited, instead of capping at one (now safe because the autopilot single-steps; also lands multi-tile LLM presses correctly). **Validated free on run #15's real frames (`eval/replay_drift.py`): cap-at-one drift 40.2% → 0** (residual = pre-existing post-warp re-baselines + multi-axis LLM presses, both out of scope). 170 tests. **Lesson: when a "hard" sensor problem (dead-reckoning drift) is 96% one systematic error, measure it against the oracle before redesigning — the fix was a 2-tile vs 1-tile press model, not a Kalman filter. And the deferred-note's reason for capping (it "broke the controller") was itself the wrong model: the controller didn't need a variable-step understanding, it needed to *stop* taking variable steps.**
- **Run #16 (2026-06-20, paid) — the interior-nav drift fix WORKS live; the bottleneck moved to AFFORDANCE discovery. Report `reports/2026-06-20-live-run-16-interior-nav-drift-fix-end-to-end-re-run.md`.** Re-ran the exact run-#15 config (text-only hybrid from `start.state`) with the drift fix. **Navigation is solved live:** dead-reckoned pose vs the RAM oracle drifted **2.9% (vs run #15's 40.2%)**, and — the decisive number — **only 4% across 149 same-segment move-pairs INSIDE the lab (map 40)**, the tight room that corrupted run #15 after a few moves. The agent navigated `38→37→0→40` and walked **up to Oak's tile at the top of the lab** (41 distinct lab tiles) — past run #15's wall. **But it never got the starter** (`in_battle` 0 all 618 steps, budget-cap halt at 100 wakes): the perceiver models pure geometry and has **no representation of interactables** — Oak and the Pokéballs are non-walkable tiles, so they read as *walls*, never frontiers; the autopilot only chases frontiers (wanders), and the text-only LLM (no vision, no "a ball is here" signal) **confabulates** a "lab maze / staircase / exit" and GOTOs phantom cells — neither layer ever tries the one correct action, *face a ball and press A*. **The bottleneck is the same shape one level up: run #15 = "can't navigate the interior"; run #16 = "navigates fine but can't discover/act on what's IN it."** The geometry model says *where you can go*; nothing says *what you can act on*. **NEXT = an interaction-discovery primitive:** when the autopilot exhausts frontiers (the `[wake:stuck]` trigger), face each adjacent *wall* and press A — a mode-change/decoded-text means that wall is an interactable (record it as an ROI, wake the LLM with it). Free, vision-free, world-agnostic, and it *replaces* wasted stuck-wakes. *Process: caught this live by checking RAM map_id mid-run — the LLM narrated "in Oak's lab" while RAM said Pallet, i.e. the narration confabulates; trust the oracle, and single-press makes the autopilot ~2× slower per step (1 tile not 2) so budget the steps accordingly.*
- **Affordance layer (interaction-probe + motion-saliency) + Run #17 (2026-06-20, paid) — got the starter COLD and WON the rival battle; the affordance layer is validated. Report `reports/2026-06-20-live-run-17-affordance-layer-probe-saliency-got-the-starter.md`.** The run-#16 wall was that the agent navigates the lab but can't TRANSACT the starter: the perceiver models pure geometry, so Oak/the Pokéballs (non-walkable tiles) read as *walls*, never frontiers — the autopilot wanders, the LLM confabulates. Two complementary, free, pixels-only signals close it: **(1) motion-saliency** — on a CAMERA-STATIC frame (no scroll) a changed off-centre tile is a moving entity (an idle-animating NPC); a connected-component size filter keeps lab NPCs and rejects animated terrain (data-first: Pallet water 35→7 events, lab NPCs preserved — `eval/inspect_motion`); surfaced as `spatial_memory.rois` + an LLM hint. **(2) interaction-probe** — when frontiers are exhausted, face each adjacent WALL and press A (turns in place → no drift); a mode-change/decoded-text = an interactable (record as ROI). **Run #17 (cold from `start.state`): the probe fired 23× and got the starter (Squirtle), then WON the rival battle** (vs Bulbasaur, a type DISadvantage; `in_battle` 2→0 sustained at 842, stayed on map 40 = no blackout) — **first start→starter→win in one run**, and navigation+starter cost only **~6 of 69 wakes** (the probe is autopilot, not a wake; 227 free advances). **Lesson: the geometry map says WHERE you can go; affordances say WHAT you can act on — and "a wall might be an interactable, so knock" + "what moves while the camera is still is alive" are both general (no game facts) and free.** *Caveats:* the place-graph stayed UNRELIABLE (it merged the lab into Pallet, area 0 — confirmed run #16 + free `probe_loop`; run #15 instead FRAGMENTED the lab into 5 places) yet the agent succeeded anyway, so **place-detection reliability is the confirmed #1 nav blocker** (misses warps that complete on a non-directional action → merge; mints spurious places from dialog-flicker → fragment); and a NEW API failure mode halted it post-win — `invalid_request` (AnthropicException, NOT credits), 4 consecutive → the circuit breaker correctly halted. *Validated free first (`eval/probe_loop`, scripted-A fallback) — which caught that cross-place's premise (a separate lab place) doesn't hold, before the paid run; validate the loop, not the primitives.* **Also audited the prompts for RECALL: `POKEMON_SYSTEM` + aria's seed (`goals.md`/`core_memory.md`/`lessons.md`) inject a walkthrough (the full route "leave Pallet → Pewter," the type chart, gym order) — so "exit Pallet" is RECALL, not decode; the decode-aligned alternative is cross-place exploration (built, behind the place-graph fix).** Survey of how others do agent vision (Claude/Gemini Plays Pokémon use RAM overlays — which we forbid; academia: raw-screenshot-VLM ≈ random; cost-optimal designs are hybrid LLM-vision-rare — our architecture) confirmed the plan; off-the-shelf detectors don't fit 160×144 sprites (use classical/structural CV + the LLM's eyes selectively).
- **S1 — the harness cost-breaker (2026-06-20, harness-only, free-validated; branch `feat/cost-breaker`).** The standing precondition for resuming paid runs (NO paid run was to happen until this landed). Four guardrails, all free to validate: **(1) per-call `prompt_tokens` to the console** — the foundational one: `_openai_complete` was *discarding the `usage` block the backend already returns*, so we were flying blind on the exact tokens/wake the cost investigation had to INFER; now the brain captures it and prints `[tokens] prompt=… (cached=…) completion=… ~$… | run ~$…` every wake. **(2) per-wake prompt-token cap** (`--max-prompt-tokens`, default 32000) — a runaway-bloat tripwire sized above the current ~13k/wake baseline. **(3) estimated-spend circuit-breaker** (`--max-cost-usd`) — the brain accrues `total_cost_usd` from real usage × Haiku-4.5 pricing and the drivers halt at the ceiling. **(4) a wake-denominated watchdog** (`--stuck-wakes`, default 30) — halts on N LLM wakes with no oracle progress. **Non-obvious learnings:** (a) *`--max-llm-calls` (wake COUNT) is a poor cost proxy* — a bloated wake (POKEMON_SYSTEM re-sent ~7×, ~13k tok) costs many times a lean one, so the true ceiling must meter tokens×price, not count wakes. (b) *The step-watchdog is fooled by free auto-advance* — run #15's aimless-but-real wandering ticked RAM coverage and placated `--stuck-steps`; once auto-advance inflates steps-between-wakes, a **wake**-denominated watchdog is the honest complement (it bills the agent for *flailing that costs money*, not for *moving*). (c) *Metering stayed opt-in without a protocol break* — `complete_fn` may now return `(text, usage)` OR a bare `str`; `LLMButtonBrain._invoke` normalizes both, so Ollama/gateworld/test stubs are untouched. (d) *The RAM/no-leak wall held* — the wake-watchdog lives in the DRIVER and correlates `brain.woke` with the driver's oracle progress; the brain never sees RAM. (e) Cost is an honest ESTIMATE for a *guard*, not an invoice — billed from whatever usage fields the backend reports (`prompt_tokens_details.cached_tokens` / `cache_*_input_tokens`), defaulting uncounted input to the input rate. **+21 tests (206 total); core/ stayed world-agnostic** (pricing is a model property, injectable → brain-agnostic per ROADMAP; the guards live in a new pure `core/cost_guard.py` shared by both drivers). **Testability:** the halt logic was extracted from the driver closures into pure `spend_halt_reason`/`wake_stall_halt_reason` predicates, so the breaker is proven FREE — unit tests + an end-to-end `run_episode` test where a real brain meters escalating cost and the guard halts the loop at the exact wake (no API/ROM). **Adversarial review (1 HIGH fixed):** the wake-watchdog was advertised-but-inert for `--brain llm` (a bare `LLMButtonBrain` had no `woke`) → gave it a wake counter; plus hardenings (zero `last_prompt_tokens` on a call exception; cache-field math is forward-looking — the live aria backend reports no cache split yet, so today the estimate over-bills slightly = the safe direction; per-wake cap default 32k→48k, above the observed ~30k worst case). The meters reading *real* aria usage + a live halt await the first guarded paid run (now safe — that run is itself S1's live validation).
- **S2 — constitution-first (2026-06-20, cross-repo, free-validated; harness `feat/constitution-first`, aria local).** Put `POKEMON_SYSTEM` in aria's CACHED static prefix instead of the volatile user turn — the structural fix for the cost root-cause (S1 only *meters/caps* it). **The "biggest unknown" resolved by reading the code: aria did NOT honor an inbound system message** — `openai_compat.handle()` took only the last `role=="user"` message and built its whole system prompt from its `data_dir` seed. So the mechanism is: aria now renders an inbound **system-role** message as a `constitution` block placed FIRST in `_STATIC` (→ system message #1 → litellm `cache_control: role:system` → cache breakpoint BP1), **dormant when none is sent** (the companion is byte-identical — verified); the harness sends `POKEMON_SYSTEM` as a **system message** (openai/aria backends only) rather than stapling it into the user content. **Non-obvious learnings:** (a) *the decoupling shaped the mechanism* — POKEMON_SYSTEM stays single-source in the harness (the world owns it) and travels over HTTP as a system role, so neither a cross-repo file-seed nor duplicating the text into aria's seed was needed; (b) *cache_control isn't in aria's Python* — pydantic-ai emits the two `@agent.system_prompt` fns as two system messages and **litellm-config.yaml** stamps `cache_control` on `role:system`, so a block rides BP1 purely by being first in `static_prompt` (the breakpoint marks the whole message, not a position) — adversarially runtime-traced end-to-end; (c) *backward-compat needs the explicit-vs-default distinction* — delivering the system as a message only when one is EXPLICITLY passed (not the `_DEFAULT_SYSTEM` fallback) keeps a plain openai/llamacpp caller's wire format unchanged (review MEDIUM); (d) **a behavior change that's only provable live**: the THINK/MOVE reply contract moved from the user turn to the top-of-prompt constitution — a small model *could* weight it differently, so the first paid run must re-check adherence before trusting the win. **+9 aria tests, +5 harness tests (211 harness total); aria suite has only its 4 pre-existing failures** (1 Red-persona guardrail test on the trainer branch, 3 Windows `find` env tests — both proven independent of S2 by stash-and-rerun).
- **S1 + S2 PAID-VALIDATED LIVE (2026-06-20, ~$0.07, bounded battle smoke from `rival_battle.state`, text-only hybrid).** First paid run on the S1+S2 stack (`feat/constitution-first`), after rebuilding aria's Docker image (its src is baked into the image — `build: ./aria`, NOT volume-mounted, so S2 code needs a rebuild) and `reset_aria_memory.py --yes`. **S1 confirmed:** the per-wake `[tokens] prompt=… (cached=…) completion=… ~$…` line printed against the real backend, the running cost accrued, the **budget-cap guard halted** at 10 wakes, and the run-end summary printed `61220 in / 1113 out (0 cached) ~$0.07`. **S2 confirmed:** (a) a cheap pre-probe (system message "reply PONG" → reply `PONG`) proved aria now **honors the inbound system message** (old image answered as the Red persona); (b) **THINK/MOVE adherence HELD** — the one offline-unprovable risk (the contract moved from the user turn to the cached constitution) did NOT materialize; the agent reasoned + emitted parseable moves every wake, with correct battle grounding (Charmander vs Squirtle, SCRATCH over GROWL); (c) **per-wake prompt was lean ~4–8k** (vs the documented pre-S2 ~13–30k) and grew only ~300–500 tok/wake — the transcript is NOT ballooning with replayed POKEMON_SYSTEM (dedup works). **Non-obvious learnings:** (1) *aria's prompt is dominated by its own seed, not POKEMON_SYSTEM* — a pre-reset PONG probe showed 13.6k (accumulated journal/memstore), a post-reset wake ~5k; the big lever on aria-side cost is the seed/memory size, separate from S2's dedup. (2) *the harness cost estimate is cache-BLIND* — `cached=0` every wake because aria's usage block omits cache tokens, so the `[tokens]`/`total_cost_usd` figure is an over-estimate (aria DOES cache via litellm `cache_control`; the true Anthropic bill is lower — read aria's own `usage.jsonl` for cache reads). Safe direction for a guard. (3) *from `start.state` the free autopilot explored the house for 120 steps with **0 wakes*** (never stuck, never hit dialog) — to exercise the brain cheaply, start from a fixture that forces a decision (the battle state), exactly the isolated-fixture tactic from run #4. Oracle: `in_battle=2` all 37 steps on map 40 (battle ongoing at the cap — NOT a win, not claimed). **⇒ S1+S2 are real. NEXT: S3 (β) + a longer paid run to measure end-to-end cost/wake at steady state.**
- **S3 — within-run memory → aria (β) (2026-06-20, harness-only, free-validated; branch `feat/within-run-memory-beta`).** β = aria OWNS within-run memory; the harness retires its DUPLICATE store. Implemented as `LLMButtonBrain(owns_memory=True)` (set by the aria drivers): gate OFF both the harness LESSON-buffer accumulation AND re-injection, so aria's native `<lesson>`→`lessons.md` (re-injected by aria each turn, stripped server-side before the reply) is the sole within-run lesson store; drop the `LESSON:` lines from `POKEMON_SYSTEM`; make the disconfirm SURPRISE note channel-neutral. Memoryless backends (`owns_memory=False`) keep the harness buffer byte-identical. **Scoped via a design workflow that adversarially mapped the memory machinery in both repos + the leak surface** (3 parallel mappers → synthesis). **Non-obvious learnings:** (a) *β revises a HARD LAW's letter* — the learning-boundary said "never use aria's durable memory as the within-run store"; β makes it exactly that, so the no-across-run-leak invariant became **load-bearing on `reset_aria_memory.py`** (under α a fresh per-process buffer guaranteed it for free). (b) *The leak surface is real* — the workflow found a CRITICAL (forgotten reset → same-day journal/recap leak), a HIGH (git-missing → seed-revert silently skipped, keeping mutated lessons.md/core_memory.md), MEDIUM (cross-day reflection baking a run into episodic/auto-facts), LOW (stale memstore.db). Closed with: a **fail-loud `is_clean` driver guard** (abort a fresh aria run on un-reset memory; `--allow-dirty-memory` for resume) + a **fail-hard, HEAD-verified git seed-revert** in reset. (c) *The guard's name-based `is_clean` is sufficient* because aria's `handle()` appends to `journal/` on EVERY turn (success or error, outside the try/finally) — so any un-reset state always shows a non-seed entry; the feared false-negative (a run that wrote only to the seed files lessons.md/core_memory.md) is unreachable. (d) *aria needed ZERO code change* — it already owns the `<lesson>` machinery; S3 is a pure harness-side retirement of the duplicate. (e) *Behavioral risk, provable only live:* with the `LESSON:` ask gone, the agent must now use `<lesson>` (taught only by aria's memory_protocol) — run #3 had `LESSON:` 56× / `<lesson>` 0×, so lesson AUTHORING could drop; the first β paid run must check it. **+8 tests (219 total). Kept harness-only (aria can't derive): the auto-advanced missed-text transcript, OutcomeMemory, disconfirm.** *Process: the adversarial-review subagent was cut off by a session usage limit before emitting findings, so the headline risks were self-verified (gating tests, live reset exit-0 + git-layout check, the journal-every-turn proof) rather than via the review.*
- **First β paid run (2026-06-20, ~$0.09, battle smoke from `rival_battle.state`) — the β behavioral RISK MATERIALIZED: lesson authoring dropped to ZERO.** Verified against the oracle, not narration: **`lessons.md` did NOT grow (11 lines, unchanged) — the agent authored 0 `<lesson>` tags.** With the harness `LESSON:` ask removed (S3) and only aria's memory_protocol teaching the tag, the agent stopped recording within-run lessons entirely — the exact run-#3 pattern (`<lesson>` 0× / `LESSON:` 56×) but now with NOTHING capturing them. **β retired the duplicate AND silently killed within-run lesson authoring; the harness `LESSON:` prompt was load-bearing for AUTHORING, not just storage.** S1/S2 held (lean ~4–5.6k prompts, `[tokens]` line + budget-cap guard fired, THINK/MOVE parsed). **Lesson: aria's generic memory_protocol nudge isn't enough in the trainer context — authoring needs an explicit per-world nudge.** Fix (proposed): re-add a brief "record durable lessons with `<lesson>…</lesson>`" line to `POKEMON_SYSTEM` (a game-specific prompt in `games/`, so it MAY reference aria's tag — unlike `core/disconfirm.py`, which stays channel-neutral); aria still parses + stores + strips it, so it's β-consistent (aria owns the store; the prompt only nudges authoring) and THINK/MOVE parsing is unaffected. **Secondary (oracle):** `in_battle=2` all 23 steps on map 40 — the late "Overworld → Pewter City" narration was CONFABULATION; the agent tried to *switch* Pokémon (it has only Charmander — nothing to switch to), the perceiver lost battle context in the party sub-menus, and it narrated overworld nav while stuck in the battle menu (a battle-policy + sub-menu-perception issue, separate from β). *Process win: the driver `is_clean` freshness guard + the journal-every-turn invariant both behaved as designed (the run wrote `journal/`+`usage.jsonl`, so post-run `is_clean`→dirty, correctly forcing a reset before the next run).*
- **Constitution → aria config (ADR-001) PAID-VALIDATED + the β lesson-regression FIXED (2026-06-20, ~$0.06).** Moved `POKEMON_SYSTEM` off the wire into **aria's own config** (`pokemon-red-data/constitution.md`, read via `memory.read_constitution`; the harness now sends `system=""`) so the brain OWNS its identity — the cleaner realization of the dual-process seam (the world shouldn't hand the agent its purpose every turn). **Validated live three ways:** (1) a clean-message probe (no system message) had aria reply *"Based on my constitution and goals…"* + correct THINK/MOVE — so it loads `constitution.md` itself; (2) a bounded battle run fought correctly (FIGHT→SCRATCH, *not* GROWL, no futile switching — better than the prior β run); (3) **`lessons.md` grew 11→17: the agent authored 3 `<lesson>` tags** ("pick the damaging move, not GROWL"; "Scratch still damages despite the Fire-resist; don't switch") — **the β-run's zero-lesson regression is FIXED** by putting the `<lesson>` authoring nudge back, now in `constitution.md` (a game-specific config that MAY name aria's tag) rather than in `core/`. **Non-obvious learnings:** (a) *the lesson-authoring nudge was load-bearing, and its right home is the per-deployment constitution* (config), not the harness prompt or `core/`; (b) *this is the first live evidence of the procedural-memory loop* — the agent distills a recurring task (the rival battle) into reusable rules that aria stores + re-injects within the run; that IS the S5 / skill-library seed ("improve every time you re-do a task"), still wiped per run by the learning boundary; (c) `constitution.md` is tracked as seed (`.gitignore` `!` exception + added to `reset_aria_memory.py` SEED/SEED_TRACKED) so the hardened reset keeps + git-reverts it (verified exit-0). **Docker gotcha:** the aria rebuild hit a `ghcr.io` timeout (couldn't re-resolve the `uv` base); `DOCKER_BUILDKIT=0` (legacy builder) built from the cached base offline. Oracle: `in_battle=2` all 39 steps (battle ongoing at the 12-wake cap — NOT a win).
- **Longer cold playthrough (2026-06-20, ~$0.30, 463 steps / 25 wakes from `start.state`) — surfaced the #1 blocker: the Oak STARTER-DIALOG TRAP.** First full run on the post-ADR stack (constitution-from-config, β memory). **Nav held cold** (`38→37→0→40` to Oak's lab — the drift-fix + place-graph). But the agent got **trapped at Oak's "which POKéMON do you want?" prompt**: that persistent textbox reads as an advanceable `dialog`, so the harness **auto-advances (mashes A) forever** — yet A doesn't progress it (in Red you must WALK to a Pokéball). The agent woke 11+ times, tried A/B/START/walk-away, *recorded lessons about the failure* (lessons.md 11→31 — procedural memory working), and still couldn't escape; the watchdog halted it. **Never got the starter, never reached a battle.** **THE KEY INSIGHT: #1 is a PERCEPTION problem, not a MEMORY problem** — the agent's lesson/skill memory worked perfectly (it recorded "stuck, tried everything"), but **procedural memory can't invent the right action if perception never surfaces it.** So S5 (skill library) is NOT the next build — the run identified the bottleneck as the **perception/auto-advance trap**. **Prioritized issues:** (1) [CRITICAL] auto-advance has no "did the decoded text actually change?" guard, so a non-advanceable prompt traps it (the 50-fuse only briefly breaks the loop) — fix: gate auto-advance on real text-progress, and when A stops advancing, stop mashing + let the affordance probe / navigation fire; (2) [HIGH] `context` is too coarse — perception can't distinguish "non-advanceable prompt / control regained" from "advanceable dialog" or "blocked by walls" (the agent confabulated *"boxed in at (5,-3)"* while at a menu) — the ADR's "perception is a watched bottleneck," confirmed live; (3) [MED] inefficient pre-wake nav (~294 frontier-wander steps to reach Oak) + no goal-directed steering. **Positives:** cold nav, lesson authoring, constitution-from-config, and the cost guards (~$0.30, watchdog-halt) all worked. **→ NEXT (data-first) — fix it with a SEEN-STATES / NOVELTY signal, not a 1-step check (David's steer): the right question is "have we SEEN this state already?" (stuck = REVISITING a seen state / a cycle; progress = NOVEL states), which is strictly more general than "did A advance the text?" (the latter misses a 2-state cycle like Oak's-line↔prompt). It UNIFIES the three signals we built piecemeal — the occupancy map ("seen this pose?"), `OutcomeMemory`, the disconfirm detector — whose shared gap is they don't see the SCREEN/dialog state (`state_signature` excludes `screen_text`; auto-advance resets the progress streak every A-press). Build a bounded seen-states memory over the full observation (incl. `screen_text`) in `core/` (System 1, harness-owned per ADR-001 = intrinsic-motivation/curiosity); gate auto-advance on it (advance only on NOVEL text → on a revisit, STOP + let run #17's affordance probe walk to a ball; the probe already gets the starter, the trap PREEMPTS it). Steps: capture Oak's "which POKéMON?" frames (frozen vs 2-state cycle? — sets the shape) → build the signal → re-run from `start.state`.** Report `reports/2026-06-20-live-run-longloop.md`; the `novelty-signal` memory has the full plan.
- **Seen-states / novelty signal — the Oak-trap fix BUILT + free-validated (2026-06-21, harness-only; branch `feat/novelty-signal`: `0faa518`→`b147721`→`ce9cc4a`, 233 tests).** Closed the longloop #1 blocker with the signal David steered toward. **Data-first first (the decisive step):** replaying the longloop oracle (`eval/inspect_longloop_trap.py`) proved the trap is a **~6-state CYCLE, not a frozen frame** — the settled `'which POKéMON do you want?'` recurs **10×**, pose frozen at (5,3), `in_battle` never fires. That's *why* a 1-step "did A advance the screen?" check fails: the decoded text changes on every A-press (`…want?`→blank→`OAK? Now…`), so only "have I been in this state before?" catches it. **The non-obvious engineering subtlety — count VISITS (rising-edge), not raw occurrences.** A settled textbox is HELD across consecutive observations *within one legit dialog* (the Pallet "OAK: Don't go out!" frame held ×4 at step 300); a naive occurrence-counter would false-trip there. Counting visits — only a key that DIFFERS from the immediately-previous one increments; `observe(None)` on blank/overworld frames breaks a held run — makes the held frame 1 visit and the Oak loop (same settled frame returned-to in *separate* visits) trip at the Kth. **Why the existing `_ADVANCE_FUSE=50` never caught it:** it counts CONSECUTIVE advances and resets on the `overworld` frames the cycle interleaves between A-presses, so it never reached 50; visit-counting is immune. **Build:** `core/novelty.py` `NoveltyMemory` (counts visits to caller-supplied keys; world-agnostic, fresh per run — the `screen_text`-aware superset of `state_signature`, the first concrete step unifying the occupancy map / OutcomeMemory / disconfirm); `HybridBrain` gates auto-advance on `cycling = visits((state_signature, screen_text)) >= _CYCLE_REVISITS(3)`. **Architecture call (with David): detection = System 1, response = System 2.** A reflex can't depend on the resource it gates (if detection were in S2 you'd wake S2 every frame to decide whether to wake S2 — the cost blowup we fight), and ADR-001 already names *novelty* as S1's escalation trigger; so S1 detects the cycle for free and defers UP with a **pure-fact `cycle_note`** ("You are repeating a state you have already seen — your last action made no progress") — **no suggested action**: aria owns the decision (thin steering stays out of `core/`). **Validation (free, on real data):** the SHIPPED code (`NoveltyMemory` + `state_signature` + `_CYCLE_REVISITS`) replayed over the whole 463-step run trips **26×, ALL in the lab trap (map 40), first @ step 416** (the trap ran uncaught to 463), **zero false positives** in Oak's legit intro or the held-frame — `inspect_longloop_trap.py` asserts this, so the real run is now a regression guard on the shipped gate. **Backward-compatible:** every existing auto-advance test drives empty `screen_text` → dormant → byte-identical. **Deferred (David's idea, recorded):** semantic/embedding novelty (a SimHash/embedding ε-ball over the symbolic state, the standard way count-based exploration tolerates perception noise) — exact-match already separates cleanly here and a per-frame embedder risks re-introducing cost; the key-building call site is the single swap point when a run shows decode noise fragmenting the signal. **Honest status: BUILT + free-validated only; the PAID re-run from `start.state` (escape Oak's prompt → starter → rival battle → leave the lab) is the separate next step, pending David's go-ahead.**
- **Seen-states gate PAID-VALIDATED LIVE + a new downstream bottleneck (3 cold runs, 2026-06-21, ~$1.77 total, branch `feat/novelty-signal`).** Three runs from `start.state` to validate the Oak-trap fix. **HEADLINE — the #1 blocker is BROKEN: the agent got the starter (CHARMANDER) COLD in ALL 3 runs** (the longloop NEVER did). **The cycle gate is validated live:** in run 2 it fired **11× `[wake:cycle]`** when aria FROZE cycling Oak's lab dialogue, and the **pure-fact `cycle_note` worked exactly as designed** — aria reasoned *"the same line I've seen cycle before … A and B both repeat … I need to try a direction button to break out"* → `up+up+up` → walked to the Pokéballs → starter. That is the System-1-detects / System-2-decides seam working **live**: the harness reported only the fact (you're repeating a state), aria decided the recovery. **Run 1 (aria navigated out cleanly, no freeze) confirms the gate correctly stays QUIET on a moving agent** — vindicating the pose-inclusive key: it fires on a *frozen* cycle (pose constant) and is silent when the agent is escaping (pose changing), so it never interrupts a self-recovering agent. Run 2 also **reached the rival battle** (`in_battle=2`, halted at the cost cap mid-fight — the win is a known capability, runs #11–13). **NEW BOTTLENECK (downstream, SEPARATE from the Oak trap):** run 3 got the starter then got **STUCK on the nickname-entry KEYBOARD** — it answered YES to "give a nickname?", the keyboard grid is **misdetected as `battle`** context (the known full-screen-bright-menu limit in `detect_mode`), and aria woke **44× `[wake:mode]`** cycling *"Nickname grid open … DONE is to the right … navigate down then right"* without escaping (wake-watchdog halt). The rival-battle TRIGGER is also non-deterministic (reached run 2, not run 3). **Lessons:** (1) **the fix is real** — the gate fires precisely on a frozen cycle and the *bare* fact (no suggested action) is enough for aria to self-correct, which is the strongest possible evidence for the pure-fact seam; (2) regression on the recorded freeze + a *live* freeze (run 2) together validate it better than either alone; (3) **cost is honestly higher on the freeze-recovery path** — $0.12/12 wakes (clean nav, run 1) vs $0.75/57 (freeze→recover, run 2) vs $0.90/70 (run 3): the gate kills the *infinite* freeze but recovery still costs ~40–50 struggle wakes, so a **stronger "the Pokéballs are here" affordance hint** is a real efficiency target; (4) **NEXT BLOCKER = the post-starter nickname keyboard** — answer NO to nicknaming, OR fix `detect_mode`'s keyboard-as-battle misread, OR (the unifying fix that ties back to this work) extend the novelty signal beyond dialog auto-advance to a general "no-novelty across N wakes → press B / try something else" stuck-breaker that would catch the 44× keyboard revisit. Reports: `reports/2026-06-21-seen-states-validation.md`; oracles `runs/novelty_val{,2,3}/`; videos `runs/novelty_val{,2,3}.mp4`.
- **General no-novelty stuck-breaker — the seen-states principle generalized past the dialog cycle gate (2026-06-21, harness-only, FREE-validated; `feat/novelty-signal` `e960360`).** Closed the run-3 nickname-keyboard bottleneck the *right* way (David's call: which approach serves the generalizable-agent thesis). **The non-obvious correction that came from pressure-testing the data before coding:** my first instinct — "reuse the cycle gate / cycle_note for all wakes" — **would NOT have worked.** The keyboard isn't a *cycle*, it's a **HELD state** — run 3's 44 stuck frames are the *identical* `(battle, pose, screen_text='')` state, consecutively, which the cycle gate's rising-edge visit-counting deliberately collapses to **1 visit** (so it never false-fires on a normal held dialog). **So the held stuck-screen slips through a cycle detector by construction.** The right signal is **"decisions since the last NOVEL state," not "visits to a repeated state"** — one signal that unifies both shapes: a *cycle* (Oak) revisits seen states → no novelty → fires; a *persistence* (keyboard) holds one seen state → no novelty → fires; and it **self-clears on real progress** (a real battle's fresh narration is novel → resets), which is *why it's robust to the keyboard mis-reading as `battle`* — it keys on novelty, not the mode label. Built as a small separate tracker in `HybridBrain` (a seen-set of non-overworld `(sig, screen_text)` + a since-novel counter; at `_STUCK_STALE=12` it hands System 2 the **same pure-fact seam** — a `stuck_note`, no suggested action); the validated cycle gate is untouched; overworld "stuck" stays the frontier logic's job. **Free-validated on the REAL oracles (not just synthetic):** replaying the shipped logic over run 3 fires **27× — ALL in the keyboard region (first @ step 459, ~26 steps before the watchdog halt), 0 fires before it**; over run 2 (which reached a real `in_battle` battle) it fires **0× — no false-positive mid-fight**. **Lessons:** (1) *check the data shape before picking the mechanism* — "it's a loop" vs "it's a held state" looked the same in my head but are opposite for a rising-edge counter; pressure-testing the pitch against run 3's frames caught it before any code; (2) the cycle gate (cycling) and the stuck-breaker (persistence) are **complementary** — run 2's freeze fired the cycle gate (aria moving through novel dialogue) and 0 stuck-notes, run 3's keyboard fires the stuck-breaker and (1 visit) 0 cycle-notes; (3) **the FREE validation proves the SIGNAL fires correctly; it does NOT prove aria RECOVERS** from the keyboard given the bare fact — that's the paid-validation question (run 2 is suggestive: aria broke the Oak freeze from the cycle_note, but the keyboard is a harder screen). Honest next step = a paid run to see whether the `stuck_note` lets aria press B / back out of the keyboard. (Orthogonal cleanup still open: the perceiver's keyboard-as-`battle` misread — the stuck-breaker makes it non-blocking but honest mode labels are still worth fixing.)
- **ROBUSTNESS-FIRST: measured reliability, found + fixed the real #1 gap — starter acquisition 0/3 → 4/5 (2026-06-21, ~$1.77 batch + $3.07 fix-runs).** The pivot from single-run anecdotes to a reliability METRIC, and the whole diagnose→fix→confirm loop. **(1) The robustness batch (3 cold runs, full config + escalation) scored 0/3 STARTER** — the prior single "successes" (val1-3) were lucky, not reliable. 2/3 failed at interior lab nav ("boxed in by walls"); 1/3 at a starter-selection menu-misframe. **The dominant gap = reliable starter acquisition (the lab transaction), which we'd declared "validated" off SINGLE runs (#16 nav, #17 affordance) — over-claiming.** **(2) A 6-agent diagnosis workflow OVERTURNED the "odometry is broken" hypothesis** (mine, from a 75-100% pose-vs-RAM "miss" rate): `_best_shift` is SOUND (exact 1-tile matches; `detect_mode` 0 disagreements on 749 frames). The "misses" were artifacts — scripted Oak-drag TELEPORTS (>4 tiles, unrepresentable by the ±4-tile search BY DESIGN) + genuine scrolls dropped because cursor-advance is gated on a directional ACTION token, not the measured shift. **The REAL root cause is BEHAVIORAL: the autopilot jams ONE blocked move (robust1: 'right' x243, pose frozen) with no breaker and drains to the watchdog; the runs that got the starter escaped ONLY because an LLM wake re-routed off a stale wall** (val2: *"Wall data is stale… GOTO… to escape this collision deadlock"*). Secondary: the occupancy was ADD-ONLY (`walls` never `.discard`-ed), so a wall written at a phantom cell during Oak's cutscene boxed the agent in forever. **(3) Fix (committed `4f4878d`):** **#1** a repeated-no-move breaker (`core/brains.py`, `_NO_MOVE_STALL=8`) — after 8 frozen-pose overworld decisions force a STEERED wake (`nomove_note`) instead of repeating the dead move; generalizes the seen-states/novelty signal to the overworld wall-jam the cycle gate + stuck-breaker missed. **#2** self-correcting occupancy (`perceiver.py`) — a CONFIRMED move clears the traversed direction's wall both ways (re-grounding, not add-only accumulation). **#4** (raise warp threshold 30→45) was TRIED and REVERTED: free-validation found the warp/same-map residual bands OVERLAP (a real lab-area transition at ~37), so 45 would miss warps — deferred to a 2-frame-confirmation. **(4) Paid confirmation: STARTER 0/3 → 4/5** (same config, only the fix differs — a clean A/B; ≥4/5 was the target). **Fix #2 is the lever** — runs that explore now cover 41-42 lab tiles vs the baseline's 5-6 (the add-only map was the trap); the breaker fired only 1-4x as the safety net. **Run 5 pushed through to the rival battle** — the bottleneck moved DOWNSTREAM. Residual gaps: a walk-to-a-ball affordance miss (fix4 wandered 42 tiles, never transacted — 1/5) + post-starter (no Route 1 yet). **META-LESSONS (the durable ones):** (a) **measure robustness with N runs, not 1** — single-run "validation" relocated effort onto downstream polish (dialog cycle, keyboard, perception-escalation) while the agent usually failed UPSTREAM at nav; (b) **diagnose before fixing** — a multi-agent diagnosis overturned my confident-but-wrong odometry hypothesis and found the real (behavioral) fix; (c) **free-validate every fix on the recorded data** — it confirmed #1 fires exactly in the jam (not in legit walking) AND caught #4's regression risk before any paid run. Artifacts: `runs/robust{1,2,3}/` (baseline) + `runs/fix{1..5}/` (fixed); the diagnosis workflow `wf_182b5f9a-bb5`.
- **Perception-escalation (strong-VLM grounding at stuck moments) BUILT (2026-06-21, `99ae905`+`99ae905`..; opt-in `--vision-escalation`).** David's idea: on a STUCK signal (cycle gate / stuck-breaker), call a strong VLM (Sonnet via a litellm `vision-escalation` model) to GROUND a confusing screen for the cheap aria agent, which still decides+acts (the dual-process escalation TIER applied to PERCEPTION). `core/vision_escalation.py` (injectable `describe_fn`, prompt asks for a description not a decision, cached per state + per-run capped → a healthy run makes ZERO calls); surfaced as `vision_hint`; spend folded into `--max-cost-usd` (`SONNET_46_PRICING`). **Premise probe (live):** Sonnet grounds the nickname keyboard far better than Haiku (correct screen ID vs Haiku's confabulated "DONE button") — BUT its action guess (an "end marker") still misreads the Gen-1 START mechanic, so it's a grounding AID, not a magic solver. **Honest status: built + the path validated; NOT yet shown to change a paid outcome** (the robustness batch's dominant failure was nav, where escalation correctly doesn't fire — it targets confusing SCREENS, not navigation). The right test is whichever downstream screen-failure the next batch surfaces.
- **Process learnings (the honest ones):**
  - **Config gotcha:** `ARIA_DATA_DIR` must point at `pokemon-red-data` or aria runs without Red's seed (type chart / mission / curiosity goal) — silently wrong.
  - **aria src is baked into its Docker image (`build: ./aria`), not volume-mounted** — code changes to `aria/src` need `docker compose build aria` + `up -d aria` to go live; only the YAML configs + data dir are mounted. (Cost me a near-miss: the running image was 4 days old.)
  - **Memory contamination:** without a fresh data dir, last run's hallucinations poison the next; evals need a clean dir per run.
  - **Spend so far:** ~7 SEK (~$0.66; 339 turns, 569k in / 18k out). Prompt caching never hit (`cached:0`) — a cheap win left on the table.
  - **Blocker (2026-06-14):** the Anthropic key behind aria ran **out of credits** → all LLM calls 400. Free/local work (perception, autopilot, rig) is unaffected.

## Context-agnostic features ai-aria needs (to become the general framework)
The core gap: **aria is a memory+reasoning system built for conversation; it lacks the "act → observe outcome → learn" spine.** Prioritized, agnostic:
1. **Outcome feedback into memory** (action→result; learn from what *happened*, not narration) — also fixes confabulation.
2. **Working/task memory** (live "current goal + what I've tried") — stops looping, enables retry.
3. **Self-managed plan** (agent decomposes & updates its own subgoals).
4. **Observation-grounded belief check** (generalize the disconfirm gate: don't record a world-state your observation contradicts).
5. **Retry/escalation policy** (on failure: try differently → escalate → mark blocked).
6. **Budget-aware acting** (event-driven; being built in the hybrid).
7. **Usefulness-based forgetting** (keep what was referenced / led to success).

The whole framework is one small loop: `perceive → recall → decide → act → observe outcome → learn → forget`. aria has all but the **outcome loop (#1)** — the single highest-leverage agnostic feature.

---

## 2026-06-21 — Lightweight-vision probe + perception-architecture decision (design session)
Full record: `reports/2026-06-21-perception-architecture-decision.md` + `reports/2026-06-21-vision-model-probe.md`.
- **Lightweight zero-shot vision on 160×144 GB sprites:** fine-grained SEMANTICS fail across the whole spectrum
  (even Sonnet flubs species); what survives is LOCALIZATION (everyone), SCENE-class (CLIP ~1.0), and OCR
  (Florence read the title). Correct labels only for large/blocky sprites. **"ball"/round-object = the most
  FP-prone class everywhere.** **MobileCLIP2-S0 = best cheap labeler (75M, 9/10); our default ViT-B-32 was the
  WORST (5/10)** — don't judge CLIP on its weakest variant. Pure captioners (BLIP/CoCa/GIT) are *worse* than
  Florence on sprites — Florence's multi-task FLD-5B training is *why* it's robust, and multi-task costs nothing
  at caption time (one task per call). RapidOCR (~8MB ONNX) is ~30× smaller + ~5× faster than Florence and reads
  GB text usably, but the free template decoder still wins on gen1 → **OCR = template-default, RapidOCR-fallback.**
- **THE decisive learning (embedding retrieval):** a behaviour-labelled CLIP-embedding "store" predicts walkability
  **97.7%** on a temporal split — but that is **near-exact RECURRENCE (memorisation), NOT generalisation**
  (nearest-cosine ~1.0). **Leave-one-MAP-out collapses** (held-out lab **26.9% < 74.8% baseline**; by novelty:
  cosine `>0.97`→~100%, `<0.90`→≈chance). **CLIP image embedding captures APPEARANCE, not FUNCTION** — held-out
  lab-floor retrieved house *walls*. ⇒ **A cheap perceptual hash beats CLIP for the recurrence win; reserve the
  embedding's DISTANCE as a NOVELTY signal only; never trust a retrieved *label* below a high-similarity threshold.**
- **Multi-signal fusion (adversarial review, 23/24 real, 6 lenses converged):** "more overlapping signals = more
  robust" is FALSE — vision signals are *correlated failures*. **Split by RELIABILITY CLASS, not cheapness:**
  behaviourally-grounded signals COMMIT in System 1 (veto/typed-precedence); vision is ADVISORY, surfaced up with
  provenance, **never weighted-voted**, never its flat-~13% softmax; walkability stays movement-mono-source.
- **Converged architecture (= David's "minimal-fixed version" = the reviews = the data):** the **world model is an
  ONLINE behaviour-labelled `tile→function` map the agent builds AS IT PLAYS** (walk→walkable, bump→blocked,
  probe→interactable), keyed by a cheap tile **fingerprint** (generalise the occupancy map from position-keyed to
  appearance-keyed = the "don't walk every cell" speedup); CLIP/Florence = perception support + novelty only, not
  the world model; no offline data-gen needed (David: "the agent builds it as it plays").
- **META:** test the centrepiece on out-of-distribution data BEFORE building — the leave-map-out split flipped a
  97.7% "success" into "memorisation, fails to generalise." A single in-distribution number is not validation.

## 2026-06-21 — Tile-fingerprint `tile→function` map + novelty gate BUILT + free-validated (task #7)
Branch `feat/novelty-signal` (unpushed). New `core/tilemap.py` (agnostic `TileFunctionMap`: 64-bit dHash
fingerprint + behaviour-labelled observe/predict + Hamming-tolerant recurrence + `is_novel`), wired into
`games/pokemon_red/perceiver.py` (OBSERVE the faced tile on every move — walk→walkable, bump→blocked, from the
clean PRE-move frame; SURFACE advisory `tile_predictions` + `novel_tiles` + `tile_types_seen` as additive
`spatial_memory` keys). **Scope = map + novelty ONLY** (David's call): no autopilot behaviour change, no paid
run — the speedup A/B is the deferred follow-on. 269 tests (`tests/test_tilemap.py` + perceiver integration);
the frozen `core/contracts.py` is untouched (advisory rides the open `spatial_memory` dict). New
`eval/probe_tilemap.py` (numpy+PIL only — **needs no torch/CLIP**, a feature) replays recorded oracles.
- **THE headline (the cheap hash BEATS CLIP where it matters):** leave-one-MAP-out held-out **lab = 81% coverage
  @ 84% accuracy** — vs the CLIP embedding's **catastrophic 26.9%** (below baseline) on the same split. Why: Gen-1
  **indoor maps share a literal tileset**, so the same 16×16 tile pixels recur across maps; a perceptual hash
  recognises that literal identity, whereas CLIP's *learned* embedding had blurred lab-floor toward house-*walls*.
  A dumb deterministic hash is MORE faithful to "same pixels ⇒ same function" than a smart lossy embedding here.
- **Recurrence (temporal split): 99.7% coverage, 92.6% accuracy-when-known** (maj baseline 73.9%). Slightly below
  CLIP's 97.7% on the temporal split, but the leave-map-out result is the one that decides the design — and the
  hash wins it 3× over, for free + deterministically + CI-testably.
- **Tolerance calibration (Q7) — a clean surprise:** accuracy is **FLAT ~92.5% across tol 0..12**; exact-match
  (tol=0) already recognises **97.6%** of recurring tiles. So the residual ~7% error is **NOT** hash collisions —
  it's **intrinsic tile/function AMBIGUITY** (the same tile pixels were observed both walkable AND blocked, e.g. a
  floor tile vs an identical-looking tile an NPC stands on). Appearance alone can't perfectly determine function
  even within one tileset → the behavioural veto (a real bump overrides) and possibly scene-conditioning (Q2)
  matter. Default tol set to **6** (just above the same-cell animation spread p90=5, far below the cross-content
  max 27) — conservative for richer tilesets, on the coverage plateau.
- **Robustness (Q6):** settled overworld faced-tiles hash IDENTICALLY 76% of revisits; same-cell spread p90=5,
  so animation is a minor effect for SETTLED frames (the odometry settles before fingerprinting). Lower-priority
  than feared.
- **Lesson:** "cheaper" wasn't a compromise — the deterministic hash was *strictly better* than the learned
  embedding on the generalisation test BECAUSE the failure mode (a lossy embedding conflating distinct functions
  that share appearance) is exactly what an exact-structure hash avoids. Reserve learned embeddings for the one
  thing structure can't give you (a graded novelty *distance*), not for recognition the hash already nails.
- **End-to-end WIRED replay (`eval/replay_tilemap.py`, free, drives the real `perceive()` over run `fix4`'s 741
  overworld frames — vs the probe, which tested the algorithm on re-cropped tiles):** (a) the recurrence curve is
  textbook — `novel_tiles` fall **22.4→0.5 / frame** while `tile_predictions` rise **45→52 / frame** as the agent
  learns the room; (b) advisory-vs-later-confirmed-behaviour agreement is **81.2% on ADJACENT (clean faced-tile)
  cells but only 61.3% across ALL visible cells.** The gap is the honest catch: `_predict_visible` fingerprints
  EVERY visible cell using the camera-centred player=(4,4) assumption, so (i) peripheral/edge crops are noisier
  than the centred faced tile and (ii) near map BORDERS the player isn't at (4,4), so the screen→world cell mapping
  is wrong and predictions get pinned to the wrong cell. ⇒ For the task-#8 autopilot use, trust predictions for
  cells AHEAD / near the player (clean crops), detect off-centre frames, and keep the behavioural veto — don't
  trust the peripheral advisory. (This is the Q6 robustness item, now quantified live.)
- **2026-06-21 — cross-tileset hash test, ADVERSARIALLY VERIFIED (the verification overturned the first read —
  the workflow earned its keep).** On the new data (kanto1 + races + fixtures, 8817 faced-tiles) the hash
  leave-one-MAP-out looked great (Forest correctly novel 3.3%; every map ≥ its baseline; "no CLIP-style
  failure"). A 5-agent verification workflow **overturned the strong claim.** **Lesson 1 — hold out the right
  UNIT:** leave-one-MAP-out ≠ leave-one-TILESET-out; a held-out *indoor* map kept a *sibling* indoor map in the
  store, hiding the failure. Honest leave-one-TILESET-out (`eval/_verify_tileset.py`): town/route survive
  (wall-recall 84.7% / 99.5%) but **INDOOR wall-recall = 0.0% — 449/449 walls called walkable @ conf 0.94** (the
  confident-mispredict failure). **Lesson 2 — aggregate accuracy LIES for navigation; measure WALL-RECALL:**
  indoor's 80.7% acc > 67.2% baseline *hid* a 0% wall-recall because indoor is ~70% walkable (calling everything
  walkable scores well). **Lesson 3 — name the real root cause:** an all-zeros dHash ALIAS (flat/low-contrast
  tiles → hash 0) drove 82% of the miscalls (369 exact collisions to outdoor-walkable) — a fixable fingerprint
  defect, not "intrinsic ambiguity" (one lens called it benign on the *inflated* protocol; the tileset-out lens
  exposed it). Edge-crop (0.5%) + labels (98.9% RAM-agree) cleared as confounds. ⇒ cheap fixes (flatness guard +
  intensity bits) BEFORE the CLIP arm. **Process lesson: a good-looking generalisation number on the wrong
  hold-out unit is the classic self-deception — adversarially re-derive the hold-out, and pick the metric that
  matches the downstream cost (here: don't-walk-into-walls = wall-recall, not accuracy).**
- **2026-06-22 — the cheap hash FIX worked (and the richer key was a strict win, not just a safety patch).**
  Added VERTICAL gradient + a 4-bit brightness BUCKET to the dHash, with structured matching (intensity
  gated ±1; Hamming tol on the gradient only) + `predict(min_conf=, skip_flat=)` knobs. The brightness
  bucket is the all-zeros-alias cure: a flat DARK wall and flat LIGHT floor no longer share key 0.
  **Unexpected bonus: temporal acc-when-known jumped 90.9% → 97.8%** (coverage held) — the alias wasn't
  just a cross-tileset hazard, it was the dominant *within*-tileset error too, so killing it helped
  everywhere. Indoor leave-one-TILESET-out wall-miscalls 449 → 297 (default) → **2 with `skip_flat`**; lab
  leave-one-MAP-out flipped from confident-wrong (77.7%) to safe-novel (11% cov @ 98.9%). **Lesson: when a
  cheap key fails, look for the lossy COLLISION before reaching for a heavier model — a few well-chosen bits
  (vertical structure + absolute brightness) recovered what we'd have paid CLIP/torch for, and the residual
  is now genuine physics (appearance can't tell a flat wall from a flat floor), exposed as a coverage⇄safety
  DIAL (skip_flat/min_conf) rather than hidden as confident error.** CLIP deferred to It2+/complex envs.
- **2026-06-22 — task #8: the offline speedup ceiling did NOT survive closed-loop (a textbook offline↔online
  gap).** Wired predictions into `ExploreBrain` (soft-walls + fallback + veto). `eval/probe_navsave.py` on a
  FIXED recorded trajectory said skipping predicted-blocked cells avoids **76% of bumps @ <1% wrong-skip** —
  looked like a slam-dunk. `eval/closed_loop_ab.py` (the autopilot actually DRIVING the emulator, so its path
  diverges and its OWN sparse map feeds back) said otherwise: **naive skipping STRANDS the agent** (42 vs 134
  cells) — it learns "dark tile = wall" from a single early bump, then skips look-alike FLAT doorways/stairs and
  seals itself in (a self-reinforcing confirmation trap the fixed-trajectory probe can't show). **`skip_flat`
  (don't trust flat-tile predictions — a flat tile may be a door) fixes it: no strand, explores MORE than
  baseline (160 vs 134), bump-rate 27.9%→18.0% (~35% fewer bumps/step).** **Two lessons: (1) an offline metric
  computed on a FIXED trajectory is a CEILING, not a forecast — when the policy's own outputs change its future
  inputs (navigation, any closed loop), you MUST measure closed-loop; (2) the safe speedup is real but MODEST
  (~35% bump-rate), far below the 76% ceiling, because only distinctly-textured walls can be skipped safely.**

## 2026-06-22 — 3D GATE verified → It3 GREENLIT; the smoke bug UNDER-reported the signal
The cheap ViZDoom 3D gate (`eval/vizdoom_smoke.py`, `my_way_home`, GT-position oracle) was adversarially
verified by a 5-agent workflow. Verdict: **GREENLIGHT It3** — and the headline got STRONGER under scrutiny.
- **An off-by-one bug made the smoke test UNDER-report its own result.** pos/frame are logged BEFORE
  `make_action`, so the *i-1→i* transition is caused by the action at row *i-1*, but the analysis filtered
  "pure-forward" on row *i*. Fixing the alignment: advance-vs-blocked **83.7%→96.9%**, corr **+0.37→+0.59**.
  GT proof the bug (not the signal) was the limiter: under correct alignment FORWARD→Δangle **0.000°(std0)**,
  LEFT→+10.36°, RIGHT→−9.62°; the buggy alignment collapsed those to noise. **Lesson: when a logger records
  state BEFORE the action, the action↔transition pairing is off-by-one — a quiet bug that blurs a real signal
  into a marginal one. A contradicting verifier (lens 4) had it backwards; the GT angle test settled it.**
- **What's REAL:** behaviour=truth holds in 3D (actions map cleanly/deterministically to pose) + a cheap
  pixels-only advance/stuck detector (survives 5-fold CV; brightness-residualized still 94.8%, so it is NOT a
  lighting artifact — which killed the strongest counter-claim).
- **What to NOT overclaim:** it's a **binary advance/stuck + turn-direction classifier, NOT metric odometry**
  (graded-distance corr ≈ +0.02). And **whole-frame frame-diff is a scalar — it cannot tell rotation from
  translation** (100% of high-frame-diff non-translating steps were pure rotations). ⇒ the real-3D perceiver
  must be **OPTICAL FLOW**: column-shift *sign* → turn direction (~90–95%; frame-diff can't do this at all);
  expansion flow → advance (2-feat 98.3%). `eval/vizdoom_flow_ceiling.py` is the reusable ceiling probe.
- **Process lesson (again):** name the signal honestly — "cheap 3D odometry" was the seductive overclaim; the
  defensible claim is "behaviour=truth survives 3D + a cheap discrete ego-motion classifier exists." Same
  discipline as the tile-map: pick the metric that matches the downstream use, and don't let a good scalar
  number stand in for a capability (direction) it structurally cannot provide.

## 2026-06-23 — generalizable MODALITY detection + mode-aware AUTO-PLAY (the nudging crutch, removed for the common case)
- **Why (David's steer):** the plan to widen the corpus by *nudging* (a human hand-playing each game past its
  menus, then checkpointing) **contradicts the goal** — an agent that handles tasks from the screen should
  handle menus itself. So the first build became a world-agnostic capability, not a data workaround:
  `core/modality.py` (detect static/menu/gameplay from a frame pair + the buttons that produced it) +
  `core/autoplay.py` (`ModalAutoPolicy`: gameplay→random breadth; otherwise an escape ladder) +
  `record.py --smart-auto` + `eval/corpus_activity.py` (the readiness/validation gate). 21 new tests, 304 total.
- **The detector generalizes on the RIGHT distinction, behaviorally grounded.** Pokémon anchor
  (`--anchor runs/kanto1`, the one game with per-frame mode labels + RAM): **overworld+MOVED → "gameplay" 98%**;
  frozen screens → "static". The *known buttons* ground it (a direction that moves the world = gameplay) —
  the same trick as the 3D gate's FORWARD→Δangle≈0 proof, no GT needed.
- **Menu CLASSIFICATION by appearance does NOT generalize — so don't build it.** On the anchor, menu/dialog/
  battle read as low-confidence "gameplay" ~80–93% (Pokémon's white-panel cue is regional; a whole-frame
  flatness signal misses text-on-panel UI, and a uniform game background — Space Invaders' black field — fools
  it the other way). **This is the hash-cross-tileset lesson again:** an appearance classifier confidently
  mislabels across domains. Fix = **handle menus BEHAVIORALLY, not by label**: the policy repeats an escape
  move while it keeps changing the screen (advancing a dialog/title) and rotates when it stops (stepping a
  menu) — robust to the label being wrong, and it can't be confidently-wrong the way a classifier can.
- **Measured value, honestly bounded (cross-axis, `eval/corpus_activity.py`):** smart-auto flips Kirby from
  random **THIN→READY (active 44%→62%)** from the same cold boot; Space Invaders (static-sprite), Gauntlet
  (follow), Metroid (side) all reach gameplay (READY) — all 4 camera-model axes now have gameplay data. **But
  it does NOT crack a hard scripted intro:** Pokémon Red cold-boot stays THIN with smart *or* random (active
  ~35%, longest gameplay streak 5) — the name-entry keyboard needs a *goal-directed* sequence a cheap escape
  ladder won't find. ⇒ hard RPG intros use the **checkpoint/LLM fallback** (Red already has
  `runs/kanto1/checkpoint_02.state`), exactly the "rare residual" the plan anticipated.
- **Metric caveat found by looking:** `active%` is confounded by **cutscenes** — Oak's animated intro reads as
  "gameplay" because the screen is busy. So **`maxRun` (longest sustained gameplay streak)** is the better
  "did it reach real play" signal; a high active% with a tiny maxRun = stuck in an animated intro, not playing.
- **Architecture:** both modules are `core/` (world-agnostic, numpy-only), enforced by `test_import_boundaries`
  (no `games/` import) + per-module AST checks; frozen `core/contracts.py` untouched (`SymbolicState.context`
  already carries mode). The same `core/modality.py` is what a new world's perceiver reuses to set `context` —
  so this is on the *constancy* critical path, not just a recorder hack. No ADR (additive System-1 building).

## 2026-06-23 — does APPEARANCE (CLIP)/OCR classify modality cross-game? A fair test corrected me BOTH ways
- **Why:** David rejected the under-proven claim "menu classification by appearance doesn't generalize" and
  demanded the probe ([[deciding-under-disagreement]]: his gut = strong hypothesis, run the cheapest separating
  test). Built `eval/probe_modality_appearance.py` (+ `_modality_probe_run.py`): hand-labeled ~190 frames
  GAMEPLAY-vs-NOT via montage sheets across 5 leave-one-GAME-out units (**pokemon = red_random1+red_smart1+kanto1
  as ONE unit — splitting them would be same-domain leakage**, the headline trap), features = cheap numpy /
  frac_flat-only ablation / OCR text-amount / CLIP MobileCLIP2-S0, classifiers = numpy logistic + cosine
  centroid/kNN, metric = balanced accuracy. Run under `.venv-probe4`.
- **Result (the test corrected the claim in BOTH directions):**
  - **REFUTED the blanket claim:** CLIP **generalizes for GAMEPLAY-vs-title/boot** — mean bal-acc **83%**,
    ~98-100% on kirby/metroid/gauntlet, beating cheap (62%) and flat-only (74%). So "appearance is useless for
    modality" was too strong — my error was importing the fine-grained-walkability pessimism onto a coarse
    scene task.
  - **UPHELD for the hard part:** on the only two folds with REAL in-game NOT (pokemon **text-menus**, spaceinv
    **arcade-title/pause**), CLIP is **near chance — pokemon 55%/54%, spaceinv 64%/64%**. So **menu/dialog/UI-vs-
    gameplay does NOT cleanly generalize across games** (menus look too different per game; trained on one
    domain's UI, it can't flag another's). The cheap behavioral escape policy was the right call.
  - **OCR-text-amount is a POOR menu cue on GB (40% mean, 0% on Gauntlet):** gameplay HUDs are text-heavy too
    (SCORE / E-99 / WARRIOR-VALKYRIE), so "amount of text" doesn't separate menu from gameplay. This refutes the
    earlier "text = menu" hypothesis — OCR's value (if any) is reading CONTENT to NAVIGATE (e.g. locate "END" on
    name-entry), not classification.
- **Method caveats (honest):** kirby/metroid/gauntlet each had only 1 (flat boot) NOT frame, so their ~100% is
  gameplay-recognition vs a trivial flat boot, NOT menu-generalization → the MEAN is optimistic; pokemon &
  spaceinv are the honest folds. Small N (~190), wide error bars. The truly held-out 4 games were never loaded.
- **Implication for the intro-pass goal:** a generalizable cheap menu-DETECTOR (CLIP/OCR-amount) is a dead end;
  CLIP could serve as an "arrived-in-gameplay" gate (gameplay-vs-title is its strength), but cracking a hard
  scripted intro (Red name-entry) is a content-read/navigation task (OCR locate "END") or an LLM job, not
  classification. Menu *handling* stays behavioral + (for the hard residual) System-2.

## 2026-06-23 — camera-model probe: 3D ego-motion is real (oracle-verified); 2D class-ID is corpus-limited
- **What:** built `eval/probe_camera_model.py` (numpy+PIL, main `uv` env) — the measure-first step before a
  generalizable odometry estimator. Per transition it computes cheap pixels-only motion features and four
  **button-grounded** axes: A1 no-input (idle motion), A2 sign (D-pad→camera-shift consistency), A3 residual
  (leftover after the best single 2D translation), A4 locality (global-vs-local change). Generalizes
  `perceiver._best_shift` (kept game-agnostic) and reuses the ViZDoom flow proxies. DEV corpus = red×2 (top-down)
  / kirby+metroid (side) / spaceinv+gauntlet (labeled "fixed") / vizdoom (3D, pose = non-leaking oracle).
- **Timing gotcha (got it right this time):** GB recorder saves frame_i AFTER act_i ⇒ transition (i-1→i) caused
  by buttons[**i**]; ViZDoom logs pose+buttons BEFORE acting ⇒ caused by buttons[**i-1**] (the exact off-by-one
  that wrecked the early smoke test). Per-source timing or A2 is garbage.
- **Result — the 3D piece is the real win, and it's ORACLE-VERIFIED:** turn direction from column-shift sign =
  **95% L/R sign SEPARABILITY** (in-sample, ≥50% by construction — the real evidence is the flow_x mean gap:
  TURN_LEFT −10.79 vs TURN_RIGHT +14.45); forward advance vs expansion-flow **corr +0.42** against ground-truth
  Δposition. Reproduces the flow-ceiling result: 3D odometry-as-discrete-classifier (turn-L / turn-R / advance)
  has legs. Whole-frame frame-diff still can't separate rotation from translation; flow can.
- **Result — 2D camera-CLASS classification is NOT yet demonstrated (leave-one-UNIT-out sib-mean 44%; Pokémon =
  ONE unit, mirroring the appearance probe, so red_random1/red_smart1 can't recognize each other and topdown is
  a singleton — a PR-#2-review fix from the earlier mislabeled 31%), and the probe told us WHY (the value of the
  run):** (1) **my own a-priori label was wrong — Gauntlet II is a follow-SCROLLER, not fixed**
  (A4=0.86 whole-frame motion vs truly-fixed Space Invaders A4=0.19); the data corrected the taxonomy. (2)
  **`red_smart1` is polluted** (A2=nan/A3=1.00/A1_fd=8.6 = stuck in Red's intro, not overworld — the known
  smart-auto-can't-crack-hard-intro). (3) **`kirby_auto1` barely scrolls** (A4=0.08) so its signature is
  unreliable, though where it DOES scroll the cue is textbook side-scroll (vshare=0.00, A2=1.00). Root cause is
  the CORPUS (1–2 games/class, two singletons, recording quality, label error) — **not** the features: the
  per-game signatures are interpretable; a naive thin-centroid cross-game classifier just can't extract a clean
  class from them yet.
- **Why it matters / how to apply:** the probe is a ready instrument; the gate to a real separability verdict is
  the **odometry corpus** with the requirements this run defined — sustained-gameplay recordings (gate with
  `corpus_activity`, drop menu-polluted runs), ≥2–3 games/class, correct camera-class labels (and likely a
  coarser camera-MOTION-type taxonomy {fixed / rigid-2D-scroll / nonrigid-3D-flow}, which is what odometry
  actually branches on). Full record: `reports/2026-06-23-camera-model-probe.md`. [[deciding-under-disagreement]]
  — measure first, don't chase the LOGO number by re-tuning.

## 2026-06-23 — odometry corpus rebuild (locomotion fix) + Doom held-out: scroll-vs-fixed separates, finer cuts don't (yet)
- **What:** acted on the camera-model probe's "corpus-limited" verdict. A 4-agent diagnosis pinned the limiter to
  LOCOMOTION sparsity (the jittery auto policy wiggles the avatar in place; the camera never pans; the A3 residual
  pins ~1.0). Fix: new opt-in `record.py --explore` (direction-persistent walk, `--hold 16`) for top-down games +
  HUMAN play (`--mode human`) for side-scrollers (they need run+jump — auto can't; `--explore` walks them into a
  wall and gets STUCK at 99% static). Added `scrollPrev` (fraction of D-pad moves that are CLEAN camera pans) and a
  held-out zero-shot test; registered **Doom (ViZDoom) as held-out**.
- **Result:** `scrollPrev` cleanly separates SCROLL (21–58%) from FIXED (0–2%) cross-game; per-frame sib-mean
  29%→45%; fixed games classify 83–96%. **Honest limits:** follow-vs-side confused (both scroll), and held-out Doom
  is NOT flagged novel (×1.3, assigned scroll_side) — a 3D turn ≈ a 2D side-pan in whole-frame flow — though the 3D
  ego-motion itself is oracle-verified (turn-sign 95%, advance-corr +0.47, IN-SAMPLE — the flow proxies were built
  on my_way_home; a true zero-shot 3D test needs a 2nd scenario). `vizdoom_smoke.py` now `set_seed`s the engine so
  the recording is reproducible (a prior unseeded run gave +0.42 — the cross-machine review discrepancy).
- **Why it matters / how to apply:** the locomotion fix was the unlock; cheap cues robustly tell scroll-from-fixed.
  Deferred (re-fit later, NOT bugs): a per-RUN classifier using scrollPrev/A4 (the per-FRAME centroid drowns in each
  scroller's non-scroll majority); axis-aware features for follow-vs-side; expansion/radial-flow to flag 3D distinct
  from side-pan. Corpus is gitignored — recipe in `eval/collect_corpus.md`. Full record:
  `reports/2026-06-23-odometry-corpus-and-doom-heldout.md`.

## 2026-06-23 — held-out verification: per-run classifier generalizes; the gate is autonomous CONTROL, not perception
- **What:** built `eval/verify_heldout.py` — the per-RUN camera classifier (`[scrollPrev, A4, vshare]`, the cheap
  closer the per-FRAME centroid couldn't manage) + a HANDS-OFF zero-shot test on the held-out set (Crystalis /
  Zelda / SML / F-1). Held-out games recorded AUTONOMOUSLY (`--explore`, no human) — human-playing a verification
  game defeats the zero-shot test and risks leakage.
- **Result (N=1 conclusive, by construction):** dev per-run leave-one-unit-out **7/7** (near-tautological — the
  features were chosen to split these units; the real evidence is out-of-corpus). Only **1 of 4 held-out games was
  drivable hands-off** — **Crystalis → follow_scroll** zero-shot, nearest by a **×1.8 margin** over side (the win
  metric is class margin, NOT distance-from-corpus). SML/Zelda/F-1 come out low-motion: **INCONCLUSIVE** if a
  scroller was predicted but the driver stalled (SML), **AMBIGUOUS** if `fixed` was predicted (Zelda flip-screen
  may be correctly fixed; F-1's car never accelerated). They test the DRIVER, not the perceiver.
- **Why it matters / how to apply:** the per-run aggregation is validated and generalizes out-of-corpus where the
  pipeline produces locomotion. **F-1 reading `fixed` is NOT a perception concern — the car never accelerated.**
  The real bottleneck is autonomous CONTROL of non-top-down games (= the agent itself, the project's end goal) —
  and the HANDS-OFF discipline is what surfaced it (hand-playing would have hidden it behind human skill). Full
  record: `reports/2026-06-23-heldout-verification.md`. [[deciding-under-disagreement]]
