# Learnings log — Red / ai-aria generalization

Running, per-iteration record (newest insights kept; a consolidated final report is assembled from
this at the end). North star: a **simple, generalizable** agent that acts on the world — generalize
across games → reality, no ROM/privileged state, **cheap** (minimal API). Pokémon Red is the probe.

---

## Iteration 01 — baseline: small LLM on raw pixels
- **What:** decoupled aria as the brain (`--backend aria`), repurposed the companion into "Red", ran Haiku on raw 160×144 frames.
- **Result:** stuck in the first room; **confabulated** (invented NPCs from furniture, mislabeled the bedroom "Oak's Lab", and wrote a *fabricated* "I exited the lab" into long-term memory).
- **Learning:** **Perception is the bottleneck, not planning.** Anything from RAM was correct; anything read from pixels was hallucinated — and a *faithful memory system amplifies bad perception* (it immortalizes the hallucination). ⇒ build a perception module; demote RAM to a scoring oracle.

## Iteration 02 — the perception module
- **What:** a role-named `SymbolicState` seam (`perceive(frame) → state`, RAM → non-leaking `oracle.jsonl`); then odometry + an occupancy map (frame-diff "did my move work?" + dead-reckoned visited/walls/frontiers).
- **Result:** **it left the bedroom** (map 38→37) — the thing baseline never did.
- **Learnings:**
  - A near-vision-free signal (frame-diff + a remembered map) is enough to navigate; you don't need to *recognize* tiles to *not loop*.
  - Role-named schema (pose / spatial_memory / affordances / last_action / confidence) = a robot's belief state ⇒ generalizes; tile-specifics hide *behind* the roles.

## Iteration 03 — measure, make it cheap, make it loop-safe
- **Measurement rig (free):** scores perception vs the oracle with a scripted brain — **no API**. Walkability **100%** (single-tile), then **99.3%** after tuning.
- **Free autopilot:** frontier exploration + BFS, **0 LLM calls** — explored **3 maps / 44 cells for $0**, where baseline burned ~100 calls and never left one room. ⇒ **the cost win: take routine movement off the LLM.**
- **Area-detection + oracle-tuned thresholds:** the tuner *honestly* showed diff-magnitude can't perfectly separate map-transitions from big in-map moves (fade-detection is the proper future signal). Odometry undercounts (~1.7 real tiles/move) under multi-tile actions — coords squashed but relative structure holds.
- **Event-driven hybrid + anti-loop:** `HybridBrain` = autopilot by default, wake the LLM only at decisions; `play_loop.py` adds a **progress watchdog** (halts when no real progress for N steps — *makes mistakes but never infinite-loops*) and a **budget guard** (cap LLM calls). The autopilot is inherently loop-free (visited-map + frontiers); the watchdog bounds the LLM.
- **Mode detection (Step 3b, free):** `detect_mode()` (overworld/menu/dialog/battle). *Methodology learning:* **look at the data before coding** — capturing + inspecting real frames revealed a trivial, reliable signal: **Gen-1 UI panels are pure-white (≥230) and the game world almost never is** (overworld ~0% near-white; START-menu panel 66%), so a near-white-by-region check separates modes for ~free on CPU. General principle: *overlay/UI vs world usually separates on one simple invariant — if you find it, you skip the ML.* Non-overworld now auto-wakes the LLM (HybridBrain). overworld/menu confirmed on pixels; battle/dialog are priors.
- **Mode detection — VALIDATED on real pixels (Iteration 03 follow-up, free):** `eval/capture_modes.py` scripts the gated opening (bedroom→1F→Pallet→Oak cutscene→lab→**pick Charmander**→**rival battle**) with a seeded random-walk + A/B mashing, using RAM (`map_id`/`in_battle`/`party_count`) *only* as a navigation aid and scoring oracle — never fed to the perceiver. This finally produced the real battle/dialog frames that were missing, turning the battle/dialog "priors" into measured results:
  - **Real battle frames: 8/8 → `battle`.** Real Oak dialog → `dialog` (eyeballed), real `START` → `menu`.
  - **Overworld (RAM-confirmed moves): 76/78 → `overworld`** (the 2 misses are Pallet frames where the tile moved *and* a sign/cutscene textbox was on screen → read `menu`; waking there is the safe call).
  - **Headline (overworld vs NON-overworld, which is what drives HybridBrain's wake): 84/86 = 97.7%.** This is the property that matters — the exact non-overworld sub-label is secondary.
  - **Bug found + fixed by looking at the pixels:** the structural "bright top AND bottom ⇒ battle" prior also fired on full-screen **white/black fade-flash** frames (an all-white starter-cutscene flash read as `battle`). Measured signature: fades have **std 0.0**, real battles std > 65 (dark sprites/HP-bars/text on the white battlefield — battles are *mostly white too*, so "white ⇒ not battle" would be wrong). Fix: a **near-uniform-frame guard** (std < 6 ⇒ transition ⇒ overworld) — removes the fade false-positives without touching real battles. *Reinforces the Step-3b principle: look at the data before coding the rule.*
  - **Known limitation (honest):** the naming-keyboard / other full-screen bright menus still read as `battle` (std is high, region-white is high — region features can't separate them from battle). Harmless for the wake decision (both are non-overworld); firming up battle-vs-menu needs *structural* detection (HP-bar/sprite templates), deferred.
- **Gating, observed for real:** the autopilot couldn't reach a wild battle because Red **gates Route 1 behind getting the starter** (Oak blocks you). A concrete instance of the dependency/gating problem — the agent literally cannot progress without the menu/dialog capability. (Reinforces the gating-probe as the right test for class-2 reasoning.)
- **Outcome loop (feature #1, free):** `OutcomeMemory` — per (situation, action) record whether the action had an *observable effect*; repeated no-effects mark an action "dead" and it's surfaced to the planner ("these did nothing here, don't repeat"). Generalizes the wall-memory to *any* action; world-agnostic (no game knowledge). The simplest real seed of learn-from-mistakes.
- **goto(target) nav (feature #2, now whole, free):** the autopilot BFS-pathfinds to a *named cell* (reusing its frontier BFS — plain shortest-path on the unweighted grid, not A*), not just the nearest frontier. The **planner→target hookup is now built and unit-tested for $0**: a woken LLM may add a `GOTO: x y` line; `LLMButtonBrain._parse_goto` reads it *only* from that dedicated line (prose like "go to the stairs" is never mistaken for a target); `HybridBrain` **persists** the destination and hands it to the free autopilot on subsequent overworld steps (name a far target *once* → drive there for free, no per-tile LLM calls), then **clears it on arrival** (pose == target). The plugin's symbolic render now lists frontier coords so the planner has concrete cells to name. Proven with a stub planner (3 tests); the only thing still waiting on credits is a *live* LLM choosing the target. *Design note:* goto pathfinds over **visited** cells only, so today it means "go back to a known place / a frontier I can see" — its reach grows the moment perception gains semantic place labels ("the gym"), with no change to this seam. This is the concrete instance of the north-star loop's `decide→act` split: the expensive brain sets intent, the cheap controller executes it.
- **Gating probe — built + free plumbing passing (this session):** a synthetic `GateWorld` (`games/gateworld/`) that isolates **class-2 means-ends reasoning with backtracking** (fetch an item → carry it back → open a gate → reach the goal) and is built to separate **reasoning from recall** — two skins of the identical world (`familiar`: key→door prior; `novel`: fragment→barrier, no prior) under one **neutral prompt**; verdict = the solve-delta. Crucially it runs the **same agent unchanged**: it speaks the GB button contract and emits the same role-named `SymbolicState`, so `HybridBrain(ExploreBrain, reasoner)`, the runner, and the gateway need zero edits. Free result (scripted *oracle* reasoner = plumbing check + upper bound): **both skins solved in 27 decisions, 6 reasoner wakes** (autopilot handled 21/27 free); `test_autopilot_alone_cannot_open_the_gate` confirms exploration alone never opens it, so class-2 reasoning is genuinely required. +10 tests (67 total). The real reasoning-vs-recall measurement (`--brain llm`) is the only credit-gated step. Spec: `reports/2026-06-15-gating-probe-spec.md`.
  - *Fidelity lesson:* a new world must honour the **motion contract** the controller assumes — `ExploreBrain`'s `[d,d]` means "turn, then move" (net 1 tile); a naive move-per-press world made it overshoot and **oscillate forever**. Matching Gen-1 turn-then-move fixed it.
  - *Finding the probe surfaced (generalization TODO):* the outcome loop's `state_signature` is **pose-centric** — picking up the item changes inventory, not pose, so the loop mis-records the `A` press as "no effect" and could mark it "dead" right when it's needed. Friction, not a blocker (the text still says "you picked up the …"), but the effectiveness signal should include inventory/state deltas.
- **Honest blocker still:** LLM-side validation (battle decisions, the live `goto` target, the live Brock slice, and the live gating-probe verdict) waits on Anthropic credits. Everything above is built + unit-tested for $0 and ready.
- **Live run #1 (2026-06-15, credits added) — stuck in the house, ~$3 wasted; full post-mortem in `reports/2026-06-15-live-run-01-postmortem.md`.** First real LLM-in-the-loop run (hybrid + aria/Haiku, 400 steps): **never left the start house** (38↔37 only), 0 progress. Root cause (data-confirmed): **interior stair-warps are low-diff (~13–29) so the perceiver never detected them as area changes** (0/25 cleared `area_threshold=60`) and **never reset its coordinate frame** (`area`=0 all 400 steps). It dead-reckoned ONE drifting map across both floors → a **permanent unreachable phantom frontier at (0,0)** → autopilot "stuck" every step from #49 → **LLM woken 351/400 (88%)**, flailing toward an impossible target (it even narrated the loop). *Crisp lesson:* **instantaneous perception is solved (399/399 walkability, modes 100%, no confabulation) — the bottleneck moved to spatial-memory integration across transitions.** *Cost lesson:* `play_pokemon.py` has **no progress watchdog** (play_loop does → would've halted ~step 60), the anti-loop guarantee has a hole (an *unreachable* frontier ≠ "no frontier"), and **prompt caching was off** (`cached_tokens=0`, 2.63M input tokens uncached). Top fixes: watchdog + frontier-abandonment + caching (cheap), then fade-based transition detection + a place-graph (the real fix).
- **Live run #2 (2026-06-15, recorded, clean-start, guarded) — SUCCESS + a new wall; report `reports/2026-06-15-live-run-02.md`.** With the seam/portal fix + guardrails, the agent **reached Oak's Lab** (oracle maps 38→37→0→40, 57 cells) for **~$0.23** (30 bounded wakes vs run #1's 351/~$3); the free autopilot drove 76/123 steps. The run-#1 spatial failure is **solved on real hardware** — the iteration's thesis validated live. *New findings:*
  - **The LLM hallucinates its location (semantic perception gap).** Ground truth = Pallet→Oak's Lab; the model narrated "Viridian City"/"Gramps"/"Poké Mart" — places it never reached. Geometry (walkability/mode) is solved; the *semantic* read of where-it-is/what's-happening is unreliable confabulation. **Always verify against the oracle, never the model's narration.**
  - **It can't transact with a scripted gate.** Oak's forced dialog pulled it into the lab, but the LLM mashed A/B/directions without grasping "let Oak finish → choose a starter." Navigate ✓, comprehend/complete ✗. Gates are also **wake-expensive** (24.4% wake rate, ~4× the free 5.7% estimate, almost all in the dialog burst).
  - **Learning loop inert — ROOT CAUSE (corrected after grounding in aria's code):** the lesson channel is **LIVE, but muzzled by our prompt.** aria parses `<lesson>…</lesson>` from every reply and appends to `lessons.md` at runtime (`aria/src/aria/memory.py:245`; advertised in `prompt.py:182`) — but our `POKEMON_SYSTEM` says "reply EXACTLY THINK/MOVE, nothing else" + `max_tokens=64`, so the model never emits a tag. Proof it's the prompt not aria: the *episodic* layer fired (`earlier_today.json` captured the failure verbatim), only the *procedural* layer was starved. *(Earlier I wrote "no channel exists" — that was imprecise; the harness `_parse` is irrelevant because aria strips lesson tags before the harness sees the reply.)* Fix, per the learning-boundary law: a **harness-owned `LESSON:` line** (plain line aria passes through, NOT `<lesson>` → that persists to `lessons.md` across runs) captured into a per-run buffer, re-injected within the run, discarded at end. Needs the muzzle + token cap lifted.
  - *Cost mechanics:* aria bills ~1.8 LLM calls per agent-decision (its pipeline), ~3.3K constitution tokens injected **uncached** every call (`cached_tokens=0`). The win came from *fewer wakes*, not cheaper calls — caching still helps at higher wake volume but isn't blocking.
  - *Process win:* clean-start now **archives before wiping** (`reset_aria_memory.py` → `aria_memory_archives/iter-NNN_<date>.zip`); the wipe verified clean by the prompt dropping 6,128→3,257 tokens. Recorded MP4 (`runs/run2.mp4`, video+audio) for review.
- **Step 1 — un-muzzle lessons into a HARNESS-owned per-run buffer (free, branch `feat/lesson-buffer`, commit `45271c4`, not pushed).** Fixes run #2's inert learning loop *within the learning-boundary law*. `POKEMON_SYSTEM` drops the "EXACTLY … nothing else" muzzle and advertises an optional `LESSON:` line; `_openai_complete` `max_tokens` 64→128. `LLMButtonBrain` parses the line (`_parse_lesson`, a sibling of `_parse_goto` — read only from a dedicated line), keeps it in a **per-run buffer** (`self.lessons`, cap 8, whole-buffer case-insensitive dedup), re-injects it on later wakes, and discards it when the object dies at run end. Deliberately a plain `LESSON:` line, **not** aria's `<lesson>` tag (which would persist to `lessons.md` across runs — forbidden). 90 tests (+14). *Process learning — adversarial review earns its keep:* a 5-dimension review-then-refute workflow surfaced three real issues I'd missed: (a) the `_parse` fallback still leaked direction words from *untagged* prose and a same-line `MOVE: x LESSON: …` produced a **spurious extra button** — fixed by trusting the MOVE line exclusively (truncate concatenated directives; only scan prose-minus-tags in the no-MOVE-line free-text path); (b) `self.goto`/`self.lesson` went **stale on a model-call exception** (a latent pre-existing `goto` bug) — fixed by resetting both at `decide()` start; (c) dedup checked only the last entry — widened to whole-buffer case-insensitive. *The review also over-fired:* ~10 of 18 "confirmed" were confirmations-of-correctness or theoretical nits; judgment still required on top of the verdicts.
- **Step 2 — disconfirm/surprise detector, the "act → observe → learn" spine (free, commit `7ae55ad`).** New `core/disconfirm.py` `DisconfirmDetector`: counts consecutive decisions with no observable change (`sig` delta) and, at a threshold, emits ONE `SURPRISE: …` note that asks for a `LESSON:` (captured by step-1's buffer); consuming it resets the streak so it nudges periodically, not every step. It enriches the note with the SymbolicState's `last_action.outcome` (`blocked`/`changed nothing`) — *the disconfirm signal was already a role in the perception schema*, so the detector just reads it. **It replaced the old inline loop-breaker** (which counted only autopilot-*stuck* wakes and reset on any mode-wake). *Key insight that justified the refactor:* the old breaker **could not catch run #2's dialog flail** — Oak's forced dialog is a *mode-wake* every step, never "stuck", so no nudge ever fired; the new no-progress signal grows on mode-wakes too and finally asks for a lesson. World-agnostic, harness-owned, discarded at run end. *Review learning:* the adversarial pass found **no code bugs** this time but three real **test-coverage gaps** (streak must survive free autopilot steps; must reset on real progress; the note must reach only the wake, never the free autopilot's context) — proof that "tests pass" ≠ "the new behavior is locked"; the highest-value tests are the ones that pin the *difference* from the thing you replaced. 101 tests (+11 over step 1).
- **Step 3a — fail-safe dialog auto-advance (free, commit `facd598`).** Mash the confirm button through a PLAIN textbox for free, waking the LLM only at a real choice — run #2's wake-burst was almost all Oak's forced dialog. *Data-first paid off again (`eval/capture_dialog.py`):* captured real START-menu/YES-NO/keyboard/dialog frames and found the trap — a YES/NO box sits in the **upper-right OVER the textbox**, and `detect_mode` calls that "dialog" (the bottom textbox dominates), so auto-advancing on the mode label alone would **mash the choice**. Measured near-white in that upper-right region cleanly separates it (plain dialog ~0.00, empty box ~0.08, YES/NO ~0.33, START menu ~0.94 → threshold 0.15). Fix: `detect_mode` relabels a textbox-with-upper-right-box as "menu" (→ wake); `HybridBrain(advance_on_dialog=True)` presses A through plain dialog free and resets the disconfirm streak (advancing IS progress the signature can't see). Validated on 272 real frames; 53 became free auto-advances. The fail-safe direction: a false "choice" costs one wake, never a mashed choice.
- **Step 3b — Gen-1 textbox font decoder, DONE (free, commits `279dd9e` + review-hardening `a3e6dcd`).** Reads textbox text from PIXELS (no RAM/VRAM) for (i) a missed-text transcript injected at the next wake and (ii) semantic grounding — the LLM now reads its actual location instead of hallucinating (run #2's headline failure). *The grid is the standard Gen-1 layout* — 2 lines at y=112/128, 8×8 cells from x=8 — and 8×8 template-matching against a 42-glyph asset (`gen1_font.json`) decodes **all 6 calibration frames AND a held-out frame at 100%**. *Decisions grounded in data:* (a) calibrate from the **dialog grid itself**, NOT the naming keyboard (its charset is on an irregular ~17px pitch → pixel-offset mismatches); (b) the blinking ▼ "more" arrow is at the FIXED bottom-right cell (17), learned → "" (dropped); (c) a **quality guard** (≥3 known chars) makes a non-textbox screen yield "" not junk; (d) unknown glyphs → '?', never a wrong guess. Wired: perceiver → `SymbolicState.screen_text` → `obs.text` + `HybridBrain` per-run transcript. *Review learning:* the step-3 adversarial pass found **no bugs** — the one "major" was a real but theoretical safety gap (a choice box sitting LOW could be missed and auto-advanced); fixed by widening the choice-detection region to just above the textbox (validated it keeps plain dialog at 0.0). *Process learning that repeated all session:* "look at the data before coding" + "verify before claiming" kept paying off — I paused 3b mid-build rather than ship a hastily-labeled (=wrong-text) table, then completed it once the calibration was validated.
- **Live run #3 (2026-06-16, recorded, clean-start, guarded) — the run-#2 wall is BROKEN; report `reports/2026-06-16-live-run-03.md`.** With steps 1–3 live, the agent **comprehended Oak's forced gate, chose SQUIRTLE, and reached the rival battle** (vs BULBASAUR) — maps 38→37→0→40, 87 cells, for **~$0.33** (40 wakes, budget-capped). Run #2 never got the starter; run #3 did. *Why it worked, mechanism-confirmed:* **dialog auto-advance handled 123 dialog frames for FREE** — the gate that burned run #2's entire 30-wake budget cost ~nothing — and the LLM was woken only at the **5 real choices** (examine ball / choose starter / nickname YES-NO) + the battle. The **textbox decoder grounded it live**: decoded straight from run-#3 frames, *"ASH received a SQUIRTLE!"* and *"ASH! Let's check out"* (uppercase gaps read as '?', never a wrong guess) — the run-#2 location hallucination is gone because the LLM read the actual events. Caching even **partly engaged** (96K cached tokens vs run #2's 0). It halted **mid-rival-battle** on the budget cap — the honest edge of our capability. *New bottleneck:* **battle-move decisions** (no FIGHT/move policy yet). *Honest gaps surfaced:* the oracle doesn't log `screen_text`/`LESSON:`s (so live grounding/lesson *content* isn't auditable from `oracle.jsonl` — add it); the decoder's uppercase coverage is partial (extend via `calibrate_font.py`). *Process win:* clean-start (archived iter-002) + guardrails + record + RAM-oracle verification held; both RAM truth (`in_battle`, map ids) and a decoded frame confirm the starter+battle, and the budget cap stopped the spend at the capability edge.
- **Process learnings (the honest ones):**
  - **Config gotcha:** `ARIA_DATA_DIR` must point at `pokemon-red-data` or aria runs without Red's seed (type chart / mission / curiosity goal) — silently wrong.
  - **Memory contamination:** without a fresh data dir, last run's hallucinations poison the next; evals need a clean dir per run.
  - **Spend so far:** ~7 SEK (~$0.66; 339 turns, 569k in / 18k out). Prompt caching never hit (`cached:0`) — a cheap win left on the table.
  - **Blocker (2026-06-14):** the Anthropic key behind aria ran **out of credits** → all LLM calls 400. Free/local work (perception, autopilot, rig) is unaffected.

## Context-agnostic features ai-aria needs (to become the general framework)
The core gap: **aria is a memory+reasoning system built for conversation; it lacks the "act → observe outcome → learn" spine.** Prioritized, agnostic:
1. **Outcome feedback into memory** (action→result; learn from what *happened*, not narration) — also fixes confabulation.
2. **Working/task memory** (live "current goal + what I've tried") — stops looping, enables retry.
3. **Self-managed plan** (agent decomposes & updates its own subgoals).
4. **Observation-grounded belief check** (generalize the disconfirm gate: don't record a world-state your observation contradicts).
5. **Retry/escalation policy** (on failure: try differently → escalate → mark blocked).
6. **Budget-aware acting** (event-driven; being built in the hybrid).
7. **Usefulness-based forgetting** (keep what was referenced / led to success).

The whole framework is one small loop: `perceive → recall → decide → act → observe outcome → learn → forget`. aria has all but the **outcome loop (#1)** — the single highest-leverage agnostic feature.
