"""Archived one-off eval probes from concluded investigations.

Kept for the record; run via `python -m eval._archive.<name>`. NOT part of the active
eval toolset (see eval/README.md). These import only the active eval modules, never each other.
"""
