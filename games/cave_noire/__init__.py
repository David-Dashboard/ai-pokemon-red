"""Cave Noire world package (the THIRD world; fixed-camera class).

Currently ships the perceiver only; the GamePlugin / sandbox / system-prompt are added when the live run
is wired. Imports only `core/` + its own modules (never a sibling game) — the import-boundary wall.
"""
